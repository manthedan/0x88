diff --git a/Cargo.toml b/Cargo.toml
index 250066a..7173660 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -26,11 +26,15 @@ policy = []
 montyformat = { workspace = true }
 rand = { version = "0.9.2", optional = true }
 rand_distr = { version = "0.5.1", optional = true }
-memmap2 = "0.9.5"
-zstd = "0.13.2"
 once_cell = "1.20.2"
 sha2 = "0.10.8"
 
+# zstd-sys cannot be cross-compiled to wasm by Apple clang, and memmap2 has no
+# wasm mmap; the wasm build loads raw nets from WASI preopened files instead.
+[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
+memmap2 = "0.9.5"
+zstd = "0.13.2"
+
 [build-dependencies]
 sha2 = "0.10.8"
 chrono = "0.4.38"
diff --git a/src/lib.rs b/src/lib.rs
index c015a2f..4fabeb9 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -4,13 +4,20 @@ pub mod networks;
 pub mod tree;
 pub mod uci;
 
+#[cfg(not(target_arch = "wasm32"))]
 use memmap2::Mmap;
 
+#[cfg(not(target_arch = "wasm32"))]
 pub struct MappedWeights<'a, T> {
     pub mmap: Mmap,  // The memory-mapped file
     pub data: &'a T, // A reference to the data in the mmap
 }
 
+#[cfg(target_arch = "wasm32")]
+pub struct MappedWeights<'a, T> {
+    pub data: &'a T, // Leaked heap copy of the weights file (wasm has no mmap)
+}
+
 #[macro_export]
 macro_rules! init {
     (|$sq:ident, $size:literal | $($rest:tt)+) => {{
@@ -65,6 +72,32 @@ pub unsafe fn boxed_and_zeroed<T>() -> Box<T> {
 
 /// # Safety
 /// Only to be used internally.
+#[cfg(target_arch = "wasm32")]
+pub unsafe fn read_into_struct_unchecked<'a, T>(path: &str) -> MappedWeights<'a, T> {
+    use std::io::Read;
+
+    let mut f = std::fs::File::open(path).unwrap();
+    let size = std::mem::size_of::<T>();
+    let file_size = f.metadata().unwrap().len() as usize;
+    assert_eq!(
+        file_size, size,
+        "File size does not match the size of the structure"
+    );
+
+    // boxed_and_zeroed allocates with the layout of T, so alignment is
+    // guaranteed; read_exact loops over the WASI fd_read chunks.
+    let mut boxed: Box<T> = boxed_and_zeroed();
+    let buf = std::slice::from_raw_parts_mut(std::ptr::from_mut(&mut *boxed).cast::<u8>(), size);
+    f.read_exact(buf).unwrap();
+
+    MappedWeights {
+        data: Box::leak(boxed),
+    }
+}
+
+/// # Safety
+/// Only to be used internally.
+#[cfg(not(target_arch = "wasm32"))]
 pub unsafe fn read_into_struct_unchecked<'a, T>(path: &str) -> MappedWeights<'a, T> {
     let f = std::fs::File::open(path).unwrap();
     let mmap = Mmap::map(&f).unwrap();
diff --git a/src/mcts.rs b/src/mcts.rs
index 4b9a6cc..cb3649d 100644
--- a/src/mcts.rs
+++ b/src/mcts.rs
@@ -395,6 +395,30 @@ impl<'a> Searcher<'a> {
 
         // search loop
         while !self.abort.load(Ordering::Relaxed) {
+            // wasm32 has no threads: run the main playout loop inline (worker
+            // threads only exist for threads > 1, which the wasm build forbids).
+            #[cfg(target_arch = "wasm32")]
+            self.playout_until_full_main(
+                &limits,
+                &timer,
+                #[cfg(not(feature = "uci-minimal"))]
+                &mut timer_last_output,
+                stats_ref,
+                &mut best_move,
+                &mut best_move_changes,
+                &mut previous_score,
+                #[cfg(feature = "datagen")]
+                &mut previous_kld,
+                #[cfg(not(feature = "uci-minimal"))]
+                uci_output,
+                #[cfg(not(feature = "uci-minimal"))]
+                multipv,
+                #[cfg(not(feature = "uci-minimal"))]
+                gui_compatibility,
+                0,
+            );
+
+            #[cfg(not(target_arch = "wasm32"))]
             thread::scope(|s| {
                 s.spawn(|| {
                     self.playout_until_full_main(
diff --git a/src/tree/half.rs b/src/tree/half.rs
index b1348cd..dbb636d 100644
--- a/src/tree/half.rs
+++ b/src/tree/half.rs
@@ -51,6 +51,16 @@ impl TreeHalf {
             let ptr = res.nodes.as_mut_ptr().cast();
             let uninit: &mut [MaybeUninit<Node>] = std::slice::from_raw_parts_mut(ptr, size);
 
+            // wasm32 has no threads: initialise sequentially.
+            #[cfg(target_arch = "wasm32")]
+            {
+                let _ = chunk_size;
+                for node in uninit.iter_mut() {
+                    node.write(Node::new(GameState::Ongoing));
+                }
+            }
+
+            #[cfg(not(target_arch = "wasm32"))]
             std::thread::scope(|s| {
                 for chunk in uninit.chunks_mut(chunk_size) {
                     s.spawn(|| {
diff --git a/src/tree/hash.rs b/src/tree/hash.rs
index 2f70c6e..2143a3a 100644
--- a/src/tree/hash.rs
+++ b/src/tree/hash.rs
@@ -53,6 +53,16 @@ impl HashTable {
     pub fn clear(&mut self, threads: usize) {
         let chunk_size = self.table.len().div_ceil(threads);
 
+        // wasm32 has no threads: clear sequentially.
+        #[cfg(target_arch = "wasm32")]
+        {
+            let _ = chunk_size;
+            for entry in self.table.iter_mut() {
+                *entry = HashEntryInternal::default();
+            }
+        }
+
+        #[cfg(not(target_arch = "wasm32"))]
         std::thread::scope(|s| {
             for chunk in self.table.chunks_mut(chunk_size) {
                 s.spawn(|| {
diff --git a/src/uci.rs b/src/uci.rs
index d65cdeb..bf46e4f 100644
--- a/src/uci.rs
+++ b/src/uci.rs
@@ -11,6 +11,21 @@ use std::{
     time::Instant,
 };
 
+/// On wasm the one-shot harness passes UCI commands as argv entries (one full
+/// command line per argument); drain those before falling back to stdin.
+#[cfg(target_arch = "wasm32")]
+fn next_arg_command() -> Option<String> {
+    use std::sync::atomic::{AtomicUsize, Ordering};
+    static NEXT: AtomicUsize = AtomicUsize::new(1);
+    let idx = NEXT.fetch_add(1, Ordering::Relaxed);
+    std::env::args().nth(idx)
+}
+
+#[cfg(not(target_arch = "wasm32"))]
+fn next_arg_command() -> Option<String> {
+    None
+}
+
 pub fn run(policy: &PolicyNetwork, value: &ValueNetwork, tcec_mode: bool) {
     let mut pos = ChessState::default();
     let mut root_game_ply = 0;
@@ -32,6 +47,8 @@ pub fn run(policy: &PolicyNetwork, value: &ValueNetwork, tcec_mode: bool) {
     loop {
         let input = if let Some(msg) = stored_message {
             msg.clone()
+        } else if let Some(cmd) = next_arg_command() {
+            cmd
         } else {
             let mut input = String::new();
             let bytes_read = io::stdin().read_line(&mut input).unwrap();
@@ -575,6 +592,34 @@ fn go(
         kld_min_gain: None,
     };
 
+    // wasm32 has no threads and a blocking stdin would deadlock the search:
+    // run the search inline; "stop" mid-search is not supported there (limits
+    // end the search instead).
+    #[cfg(target_arch = "wasm32")]
+    {
+        let searcher = Searcher::new(tree, params, policy, value, &abort);
+        let mov = searcher
+            .search(
+                threads,
+                limits,
+                true,
+                multipv,
+                gui_compatibility,
+                &mut 0,
+                #[cfg(feature = "datagen")]
+                false,
+                #[cfg(feature = "datagen")]
+                temp,
+            )
+            .0;
+        println!("bestmove {}", pos.conv_mov_to_str(mov));
+
+        if report_moves {
+            searcher.display_moves();
+        }
+    }
+
+    #[cfg(not(target_arch = "wasm32"))]
     std::thread::scope(|s| {
         s.spawn(|| {
             let searcher = Searcher::new(tree, params, policy, value, &abort);
