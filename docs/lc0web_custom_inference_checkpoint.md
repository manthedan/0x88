# lc0web custom WebGPU inference checkpoint

This checkpoint records the current custom-kernel path for the batch-8 f16 `lc0web` pack. Native LC0/BLAS and f32 ONNX remain the source-of-truth correctness baselines; these browser probes are staged subgraph checks for a future custom WebGPU runtime.

## Implemented probes and benchmarks

- `?kernelBench=1`: fixed-shape WGSL `MatMul + Add` benchmark with scalar/tiled/transposed variants.
- `?ortOpBench=1`: tiny generated ORT `MatMul + Add` comparison.
- `?qkvProbe=1` / `?qkvBench=1`: encoder0 Q/K/V projections.
- `?attentionScoreBench=1`: encoder0 QK score matmul including smolgen score bias.
- `?attentionScoreOrtBench=1`: tiny ORT attention-score comparison including smolgen bias input.
- `?softmaxBench=1`: row softmax over the 8×64 attention-score rows.
- `?attentionValueBench=1`: attention-probability × value matmul.
- `?attentionValueOrtBench=1`: tiny ORT batched attention-value MatMul comparison.
- `?attentionBlockBench=1`: QKV → QK(+smolgen) → softmax → value.
- `?attentionOutputBench=1`: attention block plus output projection, residual, and ln1.
- `?attentionOutputOrtBench=1`: tiny ORT comparison for attention-value output through output projection, alpha residual, and ln1.
- `?encoder0FfnBench=1`: encoder0 FFN from ln1 through dense1/bias/sqrrelu/dense2/bias/alpha residual/ln2.
- `?encoder0FfnOrtBench=1`: tiny ORT comparison for encoder0 FFN dense1/sqrrelu/dense2/alpha residual/ln2.
- `?encoder0BlockBench=1`: full encoder0 attention+FFN block through ln2.
- `?encoder0BlockOrtBench=1`: tiny ORT comparison for attention-value output through attention output projection/ln1 plus FFN/ln2.
- `?encoderStackBench=1&encoderLayers=N`: reusable WGSL encoder-block primitive loop over `/encoder0..N-1`, with per-block CPU f32 reference checks and optional per-block tiny f32 ONNX/ORT comparison for attention-output+FFN (`encoderStackOrt=1`, default).
- `?encoderStackHeadsBench=1&encoderLayers=N&encoderStackHeads=1`: hybrid end-to-end probe that feeds the custom WGSL encoder-stack output into tiny f32 ONNX/ORT policy and WDL value heads. The policy head covers the main 64×64 move-logit matmul path plus the promotion slice/add/remap path to the final 1858 LC0 policy logits.
- `?runtime=hybrid` / `?hybridEvaluator=1`: worker-owned production evaluator wiring for the lc0web pack. It builds the real `/input/planes` → `/attn_body` activation from LC0 112-plane input, runs the full custom WGSL encoder stack, then runs the tiny f32 ONNX/ORT mapped-policy + WDL heads and feeds the resulting 1858 logits through the normal legal-prior adapter. A persistent evaluator caches the loaded pack tensor views, encoder GPU buffers/bind groups/pipelines, ORT head session, and a WGSL smolgen-bias subgraph across evaluations. This remains the stable `lc0web-wgsl-encoder-ort-heads` backend.
- `?runtime=hybrid&headBackend=wgsl` / `?runtime=hybrid-wgsl-heads`: experimental evaluator wiring for `lc0web-wgsl-encoder-wgsl-heads`. It keeps the full encoder output on GPU and dispatches the WGSL mapped-policy+WDL heads before reading back only the final 1858 logits and WDL. This is still gated as experimental and does not replace `lc0web-wgsl-encoder-ort-heads`.
- `?hybridDrift=1&encoderLayers=10&hybridDriftLimit=N`: browser fixture route used by `npm run lc0:browser-hybrid-drift` to dump hybrid evaluations for native BLAS fixtures so policy/WDL drift can be compared against f32 ONNX and native LC0 BLAS.
- `?hybridSearchBench=1&runtime=hybrid&visits=N`: bounded browser route used by `npm run lc0:browser-hybrid-search-bench` to measure cached hybrid warm-eval latency plus fixed-visit PUCT latency/visits-per-second without promoting the backend.
- `npm run lc0:browser-hybrid-search-matrix`: local/remote matrix harness that reuses one Vite server and runs `lc0:browser-hybrid-search-bench` across head backends, visit counts, and search batch sizes, writing a full JSON artifact plus compact per-cell summaries.
- `npm run lc0:browser-hybrid-search-reuse-cache-matrix`: repeated-position benchmark harness for LC0 parity controls. It sweeps `reuseTree` on/off and worker-side `evalCacheEntries` counts, then records root-reuse counts, cache hits, neural misses, completed/requested visits-per-second, and batch-fill stats.
- `npm run lc0:browser-ci-smoke`: CI-style browser smoke harness that checks the stable ORT-heads hybrid backend, experimental WGSL-heads backend, mapped-policy probe, WGSL-heads-vs-ORT fixture comparison, and final Chrome/agent-browser/Vite leak cleanup.
- `?mappedPolicyProbe=1`: tiny synthetic WGSL mapped-policy probe. It feeds known `policy[4096]`, `K[64×256]`, promotion weights, and a 1858-entry mapping table; then it reads only the mapped-policy output and verifies both normal move copies and promotion-bias additions against a CPU f32 reference.
- `?wgslHeadsProbe=1`: isolated experimental WGSL policy/value-head probe. It dispatches the full mapped-policy path (policy dense+mish, Q/K projections, Q×Kᵀ scale, promotion rows, final 1858-entry mapping) plus the full value-head path (value embed+mish, dense1+mish, dense2, softmax) on a deterministic encoder-shaped input, reads intermediate/final buffers back, compares them to the CPU f32 head reference, and runs the existing ORT tiny-head path for context. This is not wired into the hybrid evaluator.
- `?wgslHeadsVsOrt=1&fixtureLimit=N&encoderLayers=10`: real-encoder-output comparison probe. It builds real LC0 112-plane fixture inputs, runs the full hybrid WGSL encoder stack, feeds that actual encoder output through experimental WGSL policy/value heads, and compares the full 1858 mapped policy, WDL, nonzero/nonuniform checks, and legal best move against the stable tiny ORT heads path.
- `?encoderPrefix=/encoderN`: experimental tensor-prefix override for attention-output/FFN/full-block routes so the same plumbing can target later encoder layers.

The browser page now emits a `benchmarkReport` object with browser metadata, GPU adapter info where available, pack verification mode, and timing summaries. Full encoder0 WGSL block results also include per-stage diagnostic timings for QKV projection, attention scores, softmax, attention value, output projection + ln1, FFN dense1, FFN dense2 + residual, and ln2. Matmul-style block kernels now upload QKV/output/FFN weights transposed and use small tiled workgroups for QKV, output projection, and both FFN dense layers so each block reuses activation/weight tiles instead of every output invocation walking the full K dimension alone. The shared ln1/ln2 WGSL now uses one token workgroup with a 64-lane parallel mean/variance reduction instead of one serial invocation per token. When Chromium exposes WebGPU `timestamp-query`, the encoder0 block route also reports a GPU timestamp duration for the attention+FFN command sequence. `scripts/lc0_browser_wgsl_smokes.mjs` automates the main browser smokes, parses `maxAbsError`, and surfaces encoder-block stage/timestamp timings when present. `scripts/lc0_browser_wgsl_vs_ort_webgpu.mjs` runs fresh-session, alternating encoder0-block WGSL vs ORT WebGPU measurements and marks results as non-promotional diagnostics.

## Current evidence

Recent local Chromium/WebGPU/WASM smokes on the batch-8 f16 lc0web pack passed. These are browser smoke results, not CI guarantees:

- `npm run lc0:browser-wgsl-smokes -- --no-server --only encoder0-ffn,encoder0-block --timeout 25000`
  - `FFN_BENCH_DONE`, max absolute error about `3.34e-6`.
  - `ENCODER0_BLOCK_BENCH_DONE`, max absolute error about `3.58e-6` before tiled QKV/output/FFN matmuls, about `3.34e-6` after them, and about `2.86e-6` after the parallel ln1/ln2 reduction.
- `npm run lc0:browser-wgsl-smokes -- --only encoder0-block --timeout 25000`
  - Stage-timing smoke passed with max absolute error about `3.58e-6`.
  - One local single-iteration diagnostic sample after removing the attention→FFN queue-completion boundary and switching softmax to one workgroup per row reported stage avg timings: QKV projection `1.9 ms`, attention scores `0.4 ms`, softmax `0.2 ms`, attention value `0.3 ms`, output projection + ln1 `0.4 ms`, FFN dense1 `0.4 ms`, FFN dense2 + residual `0.4 ms`, ln2 `0.2 ms`; these include per-stage queue completion overhead and are bottleneck hints, not pure GPU timestamps.
  - The same smoke validated submitting attention-output and FFN command buffers together, without an intermediate `queue.onSubmittedWorkDone()` sync, at max absolute error about `3.58e-6`.
  - On the local Chromium/WebGPU run, `timestamp-query` was available and reported an encoder0 attention+FFN GPU timestamp duration around `0.5–0.7 ms` for one queued block; synchronized readback samples were around `1.0–2.7 ms`, showing that queue/readback overhead is material.
- `npm run lc0:browser-wgsl-smokes -- --no-server --only attention-value-ort-wasm --timeout 25000`
  - `ATTENTION_VALUE_ORT_BENCH_DONE`, max absolute error about `9.54e-7`.
- `npm run lc0:browser-wgsl-smokes -- --only attention-output-ort-wasm --timeout 25000`
  - `ATTENTION_OUTPUT_ORT_BENCH_DONE`, max absolute error about `1.67e-6`.
- `npm run lc0:browser-wgsl-smokes -- --only encoder0-ffn-ort-wasm --timeout 25000`
  - `FFN_ORT_BENCH_DONE`, max absolute error about `1.91e-6`.
- `npm run lc0:browser-wgsl-smokes -- --only encoder0-block-ort-wasm --timeout 25000`
  - `ENCODER0_BLOCK_ORT_BENCH_DONE`, max absolute error about `1.97e-6`.
- `npm run lc0:browser-wgsl-smokes -- --only encoder1-block --timeout 25000`
  - `ENCODER0_BLOCK_BENCH_DONE` using `encoderPrefix=/encoder1`, max absolute error about `5.72e-6`.
  - This validates the prefix-generalized tensor plumbing against a later encoder layer's weights; it still uses the staged synthetic input/reference for that layer, not true layer-to-layer activation handoff.
- `npm run lc0:browser-wgsl-smokes -- --only encoder-stack-2-wasm --timeout 25000`
  - `ENCODER_STACK_BENCH_DONE` over `/encoder0` then `/encoder1`, max absolute error about `5.25e-6` before transposed weight uploads, about `4.29e-6` after them, and about `3.34e-6` after the parallel ln1/ln2 reduction.
  - This is the first reusable encoder-block primitive loop: it feeds each WGSL block's GPU output buffer into the next block, while validating every block against a CPU f32 reference recomputed from the actual GPU handoff activation and a tiny f32 ONNX/ORT attention-output+FFN subgraph. The smolgen/QKV/softmax/attention-value portions are still checked against the CPU f32 reference, not a full block ONNX graph.
- `npm run lc0:browser-wgsl-smokes -- --no-server --only encoder-stack-10-wasm --timeout 80000`
  - `ENCODER_STACK_BENCH_DONE` over `/encoder0` through `/encoder9`, max absolute error about `2.31e-5` before the parallel ln1/ln2 reduction and about `2.29e-5` after it; per-block ORT-vs-WGSL max absolute error was about `2.46e-5` in the earlier full-stack run.
  - This validates layer-to-layer GPU-buffer handoff across the full 10-layer encoder stack for the synthetic 64×256 activation path.
- `npm run lc0:browser-wgsl-smokes -- --no-server --only encoder-stack-heads-2-wasm --timeout 120000`
  - `ENCODER_STACK_BENCH_DONE` over `/encoder0` then `/encoder1`, then tiny f32 ONNX/ORT policy/WDL heads from the WGSL stack output. Before policy remapping was included, encoder-stack max absolute error was about `4.29e-6`, policy-head diagnostic max absolute error was about `7.75e-7`, and WDL max absolute error was about `2.98e-8`. After adding the final 1858-logit mapped policy output and parallel ln1/ln2 reduction, the same smoke reported encoder-stack max absolute error about `3.34e-6`, main policy diagnostic max absolute error about `9.54e-7`, mapped-policy diagnostic max absolute error about `1.67e-6`, and WDL max absolute error about `2.98e-8`.
- Manual split-wait browser probe: `?encoderStackHeadsBench=1&encoderLayers=10&encoderStackWarmup=0&encoderStackOrt=1&encoderStackHeads=1&ep=wasm&packVerify=0`
  - `ENCODER_STACK_BENCH_DONE` over `/encoder0` through `/encoder9`, then tiny f32 ONNX/ORT policy/WDL heads from the full WGSL stack output.
  - Representative local result: encoder-stack max absolute error about `2.31e-5`, per-block ORT-vs-WGSL max absolute error about `2.46e-5`, policy-head diagnostic max absolute error about `9.54e-7`, and WDL max absolute error about `5.96e-8`.
- `npm run lc0:browser-wgsl-vs-ort-webgpu -- --samples 2 --timeout 25000 --wgsl-iters 1 --ort-iters 2`
  - Alternated fresh browser sessions in order `wgsl, ort, ort, wgsl`.
  - ORT reported `webgpu->webgpu` with WebGPU provider accepted in both ORT samples.
  - Sample medians from that run: WGSL synchronized readback/block about `3.25 ms`, ORT WebGPU average/run about `2.55 ms`; measurement-only, not promotion evidence.
- `npm run lc0:browser-wgsl-vs-ort-webgpu -- --samples 10 --timeout 25000 --wgsl-iters 3 --ort-iters 3`
  - Alternated 20 fresh browser sessions in order `wgsl, ort, ort, wgsl, ...`.
  - ORT reported `webgpu->webgpu` with WebGPU provider accepted in all ORT samples.
  - After the softmax and queue-boundary updates, sample medians were WGSL synchronized readback/block about `1.17 ms` and ORT WebGPU average/run about `2.27 ms` (`ratioWgslOverOrt ≈ 0.51`); still measurement-only, not promotion evidence.
- `npm run lc0:browser-wgsl-vs-ort-webgpu -- --samples 4 --timeout 60000 --wgsl-iters 3 --ort-iters 3`
  - Repeated fresh Chromium/WebGPU alternating run after hybrid runtime caching; ORT reported WebGPU provider present in all 4 ORT rows.
  - Representative sample medians were WGSL synchronized readback/block about `1.10 ms` and ORT WebGPU average/run about `1.30 ms` (`ratioWgslOverOrt ≈ 0.85`); still measurement-only, not promotion evidence.
- Tiny WGSL mapped-policy probe: `?mappedPolicyProbe=1`
  - `MAPPED_POLICY_PROBE_DONE` on local Chromium 149/Apple Metal with `isFallbackAdapter=false`.
  - Synthetic normal-copy and promotion-bias mapping validates in isolation: representative max abs error `2.98e-8`, normal-copy max abs error `0`, promotion max abs error `2.98e-8`, with 1792 normal outputs and 66 promotion outputs nonzero/nonuniform.
- Isolated WGSL policy/value-head probe: `?wgslHeadsProbe=1&ep=wasm&packVerify=0`
  - `WGSL_HEADS_PROBE_DONE` on local Chromium 149/Apple Metal with `isFallbackAdapter=false`.
  - The probe is intentionally not wired into the evaluator: it dispatches the full mapped-policy path plus the full value-head path on a deterministic encoder-shaped input, reads those buffers back, and compares them to the CPU f32 head reference.
  - Representative errors after adding WGSL mapped policy: policy dense max abs error about `9.83e-7`, main 64×64 policy-logit max abs error about `8.64e-7`, mapped-policy max abs error about `8.64e-7`, value embed max abs error about `3.28e-7`, and WGSL WDL max abs error about `5.96e-8`. Policy dense, policy logits, mapped policy, value embed, and WGSL WDL readbacks were all nonzero and nonuniform. The existing tiny ORT head path on the same input produced WDL `[0.2996, 0.3884, 0.3120]` with WDL-vs-reference max abs error about `2.98e-8`.
- WGSL heads vs ORT heads on real hybrid encoder output: `?wgslHeadsVsOrt=1&fixtureLimit=9&encoderLayers=10&ep=wasm&packVerify=0`
  - `WGSL_HEADS_VS_ORT_FIXTURES_DONE` on local Chromium/WebGPU for all 9 available native fixture records.
  - The probe uses the full real LC0 112-plane input path and full 10-layer hybrid WGSL encoder stack, then compares experimental WGSL heads against the stable ORT heads on the actual encoder output. Best moves matched ORT heads on `9/9`; max mapped-policy abs diff was about `3.10e-6`, max WDL abs diff about `1.19e-7`, and all WGSL mapped-policy/WDL outputs were nonzero and nonuniform under the route's gates.
- Experimental WGSL-heads evaluator smoke: `?hybridDrift=1&headBackend=wgsl&encoderLayers=10&hybridDriftLimit=1&ep=wasm&packVerify=0`
  - `HYBRID_DRIFT_DONE` with backend `lc0web-wgsl-encoder-wgsl-heads` on the startpos fixture, best move `d2d4`, and final WDL/policy readbacks nonzero/nonuniform via runtime gates. This validates the separate backend string and worker route while preserving stable `lc0web-wgsl-encoder-ort-heads` as the default.
- Post-WGSL-heads local + Mac mini matrix for experimental `lc0web-wgsl-encoder-wgsl-heads`:
  - Drift parity, local Chromium/WebGPU: `npm run lc0:browser-hybrid-drift -- --head-backend wgsl --limit 9 --timeout 300000 --baseline-mode serial` and `--limit 16` both completed. The available fixture set still contains 9 fixtures, so both evaluated 9. Best moves matched f32 ONNX and native BLAS on `9/9`; representative max drift ranges were f32 WDL `0.000156–0.000689`, native WDL `0.00189–0.00234`, f32 top-prior `0.000737–0.01665`, native top-prior `0.000824–0.01620`.
  - Drift parity, Mac mini Chromium/WebGPU: the same `--head-backend wgsl --limit 9` and `--limit 16` serial runs completed, again evaluating 9 fixtures. Best moves matched f32 ONNX and native BLAS on `9/9`; max drift was f32 WDL `0.000156`, native WDL `0.00189`, f32 top-prior `0.000737`, native top-prior `0.000824`.
  - Search latency, local Chromium/WebGPU with `requestedEp=wasm`, `packVerification=disabled`, batch `1`, 3 timed iterations per row: visits `1` => warm eval mean `39.13 ms`, search mean `84.93 ms`, `11.77 visits/s`; visits `32` => warm eval mean `40.10 ms`, search mean `1388.97 ms`, `23.04 visits/s`; visits `128` => warm eval mean `40.83 ms`, search mean `5554.80 ms`, `23.04 visits/s`. All rows returned best move `d2d4`.
  - Search latency, Mac mini Chromium/WebGPU with `requestedEp=wasm`, `packVerification=disabled`, batch `1`, 3 timed iterations per row: visits `1` => warm eval mean `36.33 ms`, search mean `72.63 ms`, `13.77 visits/s`; visits `32` => warm eval mean `36.13 ms`, search mean `1160.80 ms`, `27.57 visits/s`; visits `128` => warm eval mean `35.77 ms`, search mean `4545.73 ms`, `28.16 visits/s`. All rows returned best move `d2d4`.
  - Cleanup check after the repeated matrix found no local or remote Chrome for Testing, `agent-browser`, Vite on port 5179, or lc0 browser Node processes left.
- Latency/caching audit after WGSL-heads wiring:
  - Added per-evaluation backend phase timing to the hybrid search benchmark (`eval.phaseTimingStats` and `eval.lastBackendTiming`) and cached/predecoded the initial input-body f16 tensors (`posEncoding`, input projection weight/bias, mul/add gates) into f32 arrays at runtime construction instead of re-decoding them inside every evaluation.
  - Also removed an avoidable WGSL-head queue barrier and combines mapped-policy/WDL readback copies into the already-submitted encoder+heads command stream. The WGSL-head path now reads back only `(1858 + 3) * 4 = 7444` bytes, while the stable ORT-head path still reads back the full encoder output (`64 * 256 * 4 = 65536` bytes) before ORT heads.
  - Follow-up readback attack: the WGSL-head path now copies mapped policy and WDL into one contiguous readback buffer and performs one `mapAsync` instead of mapping two small buffers. Timing reports include `readbackMapCount`; the WGSL-head path reports `readbackBytes=7444` and `readbackMapCount=1`. A local Chromium/WebGPU smoke after this change (`--head-backend wgsl --eval-iters 3 --eval-warmup 1 --search-iters 1 --search-warmup 0 --visits 1`, `ep=wasm`, `packVerify=0`) returned `d2d4`, warm-eval mean about `24.13 ms`, and last synced readback about `13.48 ms`. This confirms correctness of the consolidated readback but also shows queue/map synchronization remains noisy and dominant; double-buffering/deferred readback still needs a workload that can overlap multiple independent positions.
  - Local Chromium/WebGPU audit run (`--eval-iters 5 --eval-warmup 2 --search-iters 1 --search-warmup 0 --visits 1`, `ep=wasm`, `packVerify=0`) showed the previous hot-path bottleneck was CPU input activation construction: before caching it averaged about `27.9 ms` of a `41 ms` warm eval. After caching, input build dropped to about `2.3–2.7 ms`; warm eval mean dropped to about `12.18 ms` for experimental WGSL heads and `14.52 ms` for stable ORT heads. Remaining synced readback/queue drain was about `9.5–9.9 ms`, making readback/synchronization the next primary bottleneck.
  - Local drift re-check after the cache/readback changes passed for both backends on 9 fixtures: WGSL-heads and ORT-heads each matched f32 ONNX and native BLAS best moves on `9/9`.
- Post-merge LC0 search parity matrix on the custom hybrid path:
  - Added `npm run lc0:browser-hybrid-search-matrix` and ran it locally and on the Mac mini after merging `main` search parity into `lc0-webgpu-pivot`. Matrix artifacts: `/tmp/lc0_postmerge_search_matrix_local.json` and `/tmp/lc0_postmerge_search_matrix_remote.json`.
  - Matrix shape: backends `ort,wgsl`, visits `1,32,128`, batches `1,2,4,8`, 3 warm eval iterations and 3 fixed-visit searches per cell, `ep=wasm`, pack verification disabled, 10 encoder layers. All cells completed with `stopReason="visit-budget"`, `completedVisits=requestedVisits`, and `rootReused=false` because this fixed-root benchmark intentionally measures fresh searches rather than reused subtrees.
  - Local Chromium/WebGPU, best batch per backend/visit: ORT heads `1v b4 34.36 visits/s`, `32v b8 66.12 visits/s`, `128v b8 59.50 visits/s`; WGSL heads `1v b1 35.70 visits/s`, `32v b8 62.65 visits/s`, `128v b2 85.28 visits/s`. Mean warm eval by group stayed about `15.8–17.5 ms` for ORT heads and `11.9–12.2 ms` for WGSL heads. One initial local WGSL-heads `128v/b1` cell returned `g1f3`; an immediate same-cell rerun returned `d2d4` at `84.43 visits/s`, and the other local/remote cells returned `d2d4`.
  - Mac mini Chromium/WebGPU, best batch per backend/visit: ORT heads `1v b2 40.42 visits/s`, `32v b4 90.88 visits/s`, `128v b4 93.81 visits/s`; WGSL heads `1v b8 56.26 visits/s`, `32v b1 112.07 visits/s`, `128v b8 114.47 visits/s`. Mean warm eval by group stayed about `14.0–15.2 ms` for ORT heads and `10.5–10.8 ms` for WGSL heads. All Mac mini cells returned best move `d2d4`.
  - Batch-size effect is workload/host dependent. Local ORT and local WGSL improved most at `32v` with `batch=8`; local WGSL `128v` preferred `batch=2/4`; Mac mini results were mostly flat across batches for `32v/128v`, especially with WGSL heads. Search stats expose `evalCalls=visits+1` and batch aggregation counts (`batchEvalCalls`, `maxEvalBatch`), but this fixed fresh-search matrix intentionally leaves `rootReused=false`.
  - Cleanup after local and remote matrix runs required killing one leftover standalone `agent-browser`/Chrome-for-Testing session from the local loop; the final leak check then showed no matching local or remote Chrome, `agent-browser`, Vite on port 5179, or lc0 browser Node processes.
- Tree reuse + eval-cache repeated-position benchmark:
  - Added worker-side `CachedLc0Evaluator` support for the hybrid evaluator, `reuseTree`/`resetBetweenSearches`/`evalCacheEntries` query plumbing in `?hybridSearchBench=1`, and `npm run lc0:browser-hybrid-search-reuse-cache-matrix` for repeated same-position cells.
  - Local Chromium/WebGPU artifact: `/tmp/lc0_reuse_cache_task1_local.json`. Matrix shape: backends `ort,wgsl`, visits `32`, batch `4`, `reuseTree=0/1`, `evalCacheEntries=0/2048`, 3 timed repeated searches per cell, no eval/search warmup, `ep=wasm`, pack verification disabled, 10 encoder layers. All cells returned `d2d4` and stopped by `visit-budget`.
  - ORT-heads results: no reuse/no cache `36.64 requested visits/s`, `99` neural misses, `0` cache hits; no reuse/cache `83.91 visits/s`, `66` cache hits, `33` neural misses, cache hit rate `0.667`; reuse/no cache `84.14 requested visits/s`, `32` completed visits across 3 searches, root reused in `2/3` timed searches; reuse/cache was similar (`82.88 requested visits/s`) because the reused root avoids re-evaluating the repeated position rather than hitting the eval cache.
  - WGSL-heads results: no reuse/no cache `42.58 requested visits/s`, `99` neural misses; no reuse/cache `104.28 visits/s`, `66` cache hits, `33` neural misses; reuse/no cache `104.66 requested visits/s`, `32` completed visits across 3 searches, root reused in `2/3`; reuse/cache `109.60 requested visits/s`. This confirms the merged LC0 controls are visible through the hybrid custom backend and separates eval-cache wins from tree-reuse wins on a repeated root.
- True batched WGSL-head evaluation:
  - `Lc0WebHybridEvaluator.evaluateBatch()` now routes the experimental `lc0web-wgsl-encoder-wgsl-heads` backend through a physical WGSL batch path instead of serial `runtime.evaluate()` calls. The batch path builds a combined CPU input tensor, performs one `queue.writeBuffer()` into a batch upload buffer, encodes all batch slots into one command buffer, and maps one combined `(batch * (1858 + 3) * 4)` readback buffer. The stable `lc0web-wgsl-encoder-ort-heads` backend remains unchanged and falls back to serial evaluation to avoid static batch-1 ORT concurrency.
  - Validation smoke: `npm run lc0:browser-hybrid-search-bench -- --head-backend wgsl --visits 8 --batch 4 --eval-iters 0 --eval-warmup 0 --search-iters 1 --search-warmup 0 --timeout 180000` completed with backend `lc0web-wgsl-encoder-wgsl-heads`, best move `d2d4`, `completedVisits=8`, `maxEvalBatch=4`, and histogram `{4:2}`.
- Batched WGSL-head benchmark harness:
  - Added `npm run lc0:browser-hybrid-wgsl-batch-bench`, which sweeps WGSL-head `evaluateBatch` modes `serial,physical` across batch sizes `1,2,4,8`. It records warm eval timing, explicit `evaluateBatch` timing/readback bytes/map count/physical batch size, fixed-visit search visits/sec, batch-fill histograms, and basic correctness gates.
  - Local Chromium/WebGPU artifact after review fix: `/tmp/lc0_wgsl_batch_task2_after_review.json` from `--visits 8 --batches 1,2,4,8 --modes serial,physical --eval-iters 1 --eval-warmup 0 --batch-eval-iters 1 --batch-eval-warmup 0 --search-iters 1 --search-warmup 0`. All cells completed requested visits with `stopReason="visit-budget"`. Serial batch-eval timing now aggregates per-slot readback maps (`batch=8` reports 8 maps and 59552 bytes), while physical batch-eval reports one map over the same bytes. At this tiny visit count, physical batch search was faster for batches `4`/`8` (~75 visits/s) than serial batch (`~51–52 visits/s`), while the explicit duplicate-position batch-eval microbench showed higher one-off setup/readback cost for physical slots; repeat with larger visits before drawing promotion conclusions.
- Experimental WGSL input-body projection:
  - Added `inputBackend=wgsl` / `--input-backend wgsl` for the hybrid evaluator. This keeps FEN/history-to-112-plane encoding on CPU, uploads compact `112×64` planes, and runs positional concat, input projection, Mish, and mul/add gates as a WGSL compute pass into the existing encoder input buffer. The default remains `inputBackend=js` while this path is experimental.
  - Validation smoke: `npm run lc0:browser-hybrid-search-bench -- --head-backend wgsl --input-backend wgsl --visits 4 --batch 2 --eval-iters 1 --eval-warmup 0 --batch-eval-iters 1 --batch-eval-warmup 0 --search-iters 1 --search-warmup 0 --timeout 180000` completed with backend `lc0web-wgsl-encoder-wgsl-heads`, `inputBackend="wgsl"`, best move `d2d4`, requested visits complete, and batch histogram `{2:2}`. The first warm eval included one-time pipeline/slot setup noise; the following batch/search timings showed compact input-build/upload around sub-millisecond scale.
- SIMD WASM LC0 input encoder prototype:
  - Added a small no-std Rust/WASM module under `wasm/lc0_input_encoder` and browser-facing artifact `public/lc0/lc0_input_encoder.wasm`. It exports fixed input/output buffers plus `lc0_encode_fen` and `lc0_encode_fen_history`; the JS bridge in `src/lc0/wasmInputEncoder.ts` writes FEN bytes into WASM memory and reads the compact `112×64` f32 planes for WebGPU upload. The Rust build uses `-C target-feature=+simd128` and vectorizes plane zeroing with wasm SIMD.
  - Validation: `node --experimental-strip-types --test tests/lc0_wasm_input_encoder.test.mjs` compares the WASM output against the existing JS `encodeLc0Classical112` path across all 10 FEN-only fixtures and all 6 explicit-history fixtures, plus no-fill and explicit single-FEN history semantics, with exact max-abs diff `0`.
- JS vs SIMD-WASM input path benchmark:
  - Added experimental `inputBackend=wasm` for the hybrid evaluator. It uses the SIMD WASM FEN/history encoder to build compact `112×64` planes, uploads those planes, and reuses the WGSL input-body projection path. The stable default remains `inputBackend=js`; `inputBackend=wasm` is only benchmark/prototype plumbing.
  - Added `npm run lc0:browser-hybrid-input-bench`, a browser harness that runs all 16 representative FEN/history fixtures and compares input backends (`js,wasm` by default). It records input-build ms, upload ms, total eval ms, and WASM bridge/copy/encode timing from the JS bridge.
  - Local Chromium/WebGPU artifact: `/tmp/lc0_hybrid_input_bench_task5.pure.json` from `npm run lc0:browser-hybrid-input-bench -- --iters 1 --warmup 0`. It completed `16/16` fixtures for `js` and `wasm`. Mean backend timings from that short run: JS input-build `3.1209 ms`, upload `0.0156 ms`, total eval `21.8391 ms`; WASM input-build `0.0762 ms`, upload `0.0053 ms`, bridge/copy `0.0328 ms`, wasm encode `0.0175 ms`, total eval `27.8531 ms`. This shows the WASM encoder itself is much faster than current JS CPU projection, but the full WASM+WGSL-input path is not yet promotion evidence because total eval stayed slower in this run.
- SIMD WASM legal-prior prototype:
  - Added a second small Rust/WASM module under `wasm/lc0_legal_priors` and browser artifact `public/lc0/lc0_legal_priors.wasm`. It owns FEN parsing, legal move generation with king-safety filtering, LC0 policy-index lookup using a generated compact policy table, legal softmax, descending candidate sort, and optional top-K output. The JS bridge in `src/lc0/wasmLegalPriors.ts` passes only FEN bytes plus the mapped-policy logits buffer and reads compact UCI/index/prior/logit arrays plus timing.
  - Validation: `node --experimental-strip-types --test tests/lc0_wasm_legal_priors.test.mjs` compares the WASM legal-prior output against JS `legalPolicyPriors` over all 10 representative FEN fixtures and all 6 explicit-history final FENs, including castling, en-passant, promotions, checks, top-K output, stale castling-right rejection, and temperature-scaled `Lc0LegalPrior.logit` semantics.
  - Added opt-in browser/runtime wiring via `legalPriorsBackend=wasm` / `--legal-priors-backend wasm`. The hybrid evaluator default remains `legalPriorsBackend='js'`; the WASM path reports `legalPriorsBridgeCopyMs`, `legalPriorsWasmRunMs`, and `legalPriorsWasmTotalMs` in backend timing. Local smoke at ORT heads (`visits=1`, one warm eval/search) matched JS best move `d2d4` and exposed the new timing fields; WGSL heads with `inputBackend=wasm&legalPriorsBackend=wasm` also completed the same small smoke. Fixture-parity smoke artifact `/tmp/lc0_hybrid_fixture_parity_wasm_legal_smoke.json` covered a nodes32 fixture with WGSL heads + WASM input + WASM legal priors and matched native/depth baseline.
  - Focused legal-prior search artifact: `/tmp/lc0_legal_priors_focused_summary.json` aggregates 8 browser fixture-parity runs over all 16 native fixtures at `visits=32`, `batch=1,4`, comparing JS vs WASM legal priors for stable ORT heads + JS input and experimental WGSL heads + WASM input. All 128 cells matched native best moves. WASM legal priors reduced reported legal-prior CPU time in every pair, but stable ORT-head wall-clock did not improve (`+4.5%` at batch1, `+1.0%` at batch4). WGSL+WASM-input wall-clock looked better in this single run, but part of that movement coincided with lower readback sync, so keep WASM legal priors opt-in/prototype pending repeated alternating runs.
- Experimental GPU legal-prior/top-k scaffold:
  - Added opt-in `legalPriorsBackend=gpu` / `--legal-priors-backend gpu` for the experimental WGSL-head backend only. JS still owns legal move generation and LC0 index mapping, but the runtime now uploads compact legal policy indices, computes temperature-scaled legal softmax and descending candidate order in a WGSL pass, and reads back compact legal slot/logit/prior triples plus WDL instead of copying the full 1858 mapped policy to CPU.
  - Readback shrinks from `(1858 + 3) * 4 = 7444` bytes to `(256 * 3 + 3) * 4 = 3084` bytes for the WGSL-head legal-prior path. This is a scaffold, not a full GPU search path: mapped-policy logits are still computed on GPU, legal indices still come from JS movegen, and JS PUCT still consumes JS legal-prior objects.
  - Browser fixture-parity smokes passed for startpos nodes32 with WGSL heads + WASM input + GPU legal priors: `/tmp/lc0_gpu_legal_priors_smoke.json` (`batch=1`) and `/tmp/lc0_gpu_legal_priors_batch4_smoke.json` (`batch=4`) both matched native/depth baseline best move `d2d4`.
  - Full fixture parity passed for all 16 representative native nodes32 fixtures: `/tmp/lc0_gpu_legal_priors_fixtures16_b1.json` (`batch=1`) and `/tmp/lc0_gpu_legal_priors_fixtures16_b4.json` (`batch=4`) both matched native and depth-1 browser baseline on `16/16` cells.
  - Focused JS-vs-GPU legal-prior A/B matrix artifact: `/tmp/lc0_gpu_legal_priors_ab_matrix.json`, WGSL heads, visits `32,128`, batches `1,4`, repeats `3`, two timed searches per cell. All 24 cells completed requested visits and returned `d2d4`. GPU legal priors reduced per-position readback bytes from `7444` to `3084`, but full search wall-clock was mixed: GPU was faster for batch-1 cells (`32v` about `0.93x` JS search ms, `128v` about `0.99x`) and slower for batch-4 cells (`32v` about `1.13x`, `128v` about `1.07x`). Search-attributed readback sync also did not reliably improve for physical batches. Decision: keep GPU legal priors as an opt-in scaffold/correctness path, not a promotion candidate yet.
- SIMD WASM vs JS PUCT feasibility audit:
  - Added `npm run lc0:puct-wasm-feasibility-audit`, a zero-neural-latency synthetic search harness that isolates the current JavaScript PUCT/tree/movegen overhead across visit and batch-size sweeps. Example local artifact: `/tmp/lc0_puct_wasm_feasibility_task7.json`.
  - Audit decision: do not start a full PUCT WASM port yet. The search loop is dominated by branchy tree/graph mutation, `BoardState`/`Move` object ownership, legal move generation, transposition/tree-reuse identity, abort/yield behavior, and PV/stat extraction rather than dense SIMD arithmetic. A partial bridge would add per-node copies and duplicate chess/search correctness logic; a useful WASM port would require moving the tree, board state, movegen, backup, and legal-prior plumbing into packed WASM-owned memory.
  - Near-term preference: keep PUCT in JS while reducing neural/WebGPU synchronization first. If CPU search becomes the bottleneck after readback/input/legal-prior work, start with JS/typed-array PUCT improvements (precompute parent visit totals instead of rescanning every edge score, object pooling, fewer `boardToFen` comparisons during tree reuse) and only promote a WASM rewrite with browser evidence showing full-search speedup without extra large copies.
- Deferred/double-buffered WGSL readback and search-scheduler prototype:
  - Refactored physical WGSL-head batch evaluation into submit/finish phases and added a bounded experimental double-buffered readback path that submits the next WGSL-head batch into one readback buffer before mapping the previous batch from the other buffer. The stable `lc0web-wgsl-encoder-ort-heads` backend is unchanged.
  - Added `npm run lc0:browser-wgsl-deferred-readback-bench`, a browser-only probe for `lc0web-wgsl-encoder-wgsl-heads` that compares immediate physical-batch readback with the deferred double-buffered sequence. Local smoke artifact: `/tmp/lc0_wgsl_deferred_readback_task8_warm.json` from `--iters 2 --warmup 1 --batch 2 --fixture-limit 2`; both modes matched best moves (`d2d4`, `e7e5`). In that short warmed run immediate was slightly faster (`108.36` vs `102.91` eval/s), so the double-buffer path started as correctness/prototype groundwork rather than promotion evidence.
  - Search now has an opt-in `batchPipelineDepth` / `--batch-pipeline-depth` scheduler experiment. For `batchPipelineDepth > 1`, PUCT collects multiple physical leaf batches with virtual visits before awaiting evaluator results; evaluators with `evaluateBatchSequence()` can then submit later batches before mapping earlier readbacks. `Lc0WebHybridEvaluator.evaluateBatchSequence()` routes WGSL-head physical batches through the existing deferred/double-buffered readback path, while ORT/static/default paths fall back to ordered awaited batches. Browser search artifacts report `batchPipelineDepth`, `batchPipelineFlushes`, and `maxBatchPipelineBatches`.
  - Local Chromium/WebGPU smoke artifact `/tmp/lc0_hybrid_search_pipeline_depth32_smoke_matrix.json` compared WGSL-head search at `32v/b4` with pipeline depth `1` vs `2`: both returned `d2d4`, completed `32/32` visits, filled `{4:8}` physical batches, and the depth-2 lane reported `batchPipelineFlushes=4`, `maxBatchPipelineBatches=2`. The single-sample timing favored depth 2 (`65.50` vs `58.97` visits/s), but this remains opt-in scheduler evidence only; repeat before drawing promotion conclusions.
  - Repeated local Chromium/WebGPU matrix artifact `/tmp/lc0_hybrid_search_pipeline_variant_matrix_r2.json` compared `batchPipelineDepth=1,2,4` at `32v/b4`, `repeats=2`, `search-iters=2`, over hand/mixed/full-TVM encoder variants and ORT vs WGSL heads. All 48 cells returned `d2d4` and completed `32/32` visits. Mean wall-clock search speedups from depth 1 to depth 4 were small/noisy for ORT heads (`hand 1.03x`, `mixed-tvm-ffn 1.09x`, `mixed-tvm-ffn-outproj 1.01x`, `tvm-packed-f16 0.95x`) but consistent for WGSL heads (`hand 1.21x`, `mixed-tvm-ffn 1.27x`, `mixed-tvm-ffn-outproj 1.26x`, `tvm-packed-f16 1.29x`). This supports continuing the search-level readback-overlap lane for WGSL heads, while keeping the scheduler opt-in and avoiding any stable ORT-head default change.
  - Added search-level backend timing attribution to PUCT stats. Search now carries `evalBackendTimingSamples`, `evalBackendTimingPositions`, `evalBackendTimingTotals`, `evalBackendTimingMeans`, and per-position means, preserving physical WGSL batch timings as one backend sample while still tracking represented positions; per-slot fields such as legal-prior CPU time are summed across the physical batch. The browser search benchmark aggregates those stats across timed searches, and the matrix summary now surfaces search-time readback/submit/encode/legal-prior means. Smoke artifact `/tmp/lc0_hybrid_search_pipeline_timing_metrics_smoke_after_review.json` at WGSL `8v/b2` showed depth 2 reducing search-attributed readback sync per backend sample from about `31.02 ms` to `26.58 ms` and per position from about `17.23 ms` to `14.76 ms`; this is attribution smoke, not promotion evidence.
  - Repeated attribution matrix artifact `/tmp/lc0_hybrid_search_pipeline_timing_matrix_r1.json` (`32v/b4`, depths `1,2,4`, repeats `2`, `search-iters=3`) completed all `48/48` cells with best move `d2d4`. ORT-head readback timing and wall-clock remained mostly flat/noisy with depth. WGSL-head depth-4 vs depth-1 wall speedups were `1.25x` hand, `1.31x` mixed TVM-FFN, `1.31x` mixed TVM-FFN+outproj, and `1.28x` full TVM; the corresponding search-attributed readback-sync drops were `1.35x`, `1.50x`, `1.49x`, and `1.43x`. Command encode and queue submit stayed tiny in this matrix, while input/legal-prior CPU time was visible but not the dominant depth-1 bottleneck. This supports the hypothesis that WGSL-head pipeline wins are mainly readback/map wait overlap, not shader-command submission savings.
  - Larger repeated matrix artifact `/tmp/lc0_hybrid_search_pipeline_timing_matrix_visits32_64_128_r1.json` broadened to visits `32,64,128` with the same depths/heads/encoder variants (`144/144` cells, all `d2d4`, all requested visits complete). WGSL-head readback-sync per backend sample consistently fell with depth: depth-1 to depth-4 drops were about `1.47x–1.83x` at 32 visits, `1.68x–2.04x` at 64 visits, and `1.95x–2.75x` at 128 visits. Wall-clock speedups remained workload/noise sensitive but favored pipelining for WGSL heads in most cells: depth-4 vs depth-1 ranged about `1.13x–1.34x` at 32 visits, `1.02x–1.51x` at 64 visits, and `1.15x–1.45x` at 128 visits. ORT-head cells again showed no reliable pipeline benefit and sometimes regressed, so the scheduler remains an experimental WGSL-head/readback-overlap lane only.
  - WGSL-head drift re-check artifact `/tmp/lc0_hybrid_drift_wgsl_heads_hand_limit9.json` matched f32 ONNX and native BLAS best moves on `9/9` fixtures for the hand encoder with WGSL heads. Max observed drift: f32 WDL `0.0003303`, native WDL `0.0017795`, f32 top-prior `0.0116494`, native top-prior `0.0113239`. This is evaluator parity for the WGSL-head path used by pipeline search; `batchPipelineDepth` itself only changes search scheduling and still needs broader fixed-search parity if non-startpos fixture search harnessing is added.
  - Deferred-readback lifecycle smoke artifact `/tmp/lc0_wgsl_deferred_readback_lifecycle_batch4_iters20.json` ran immediate vs deferred/double-buffered WGSL-head physical batches over `20` timed batches (`80` evaluations) after warmup. Best moves matched across modes, deferred improved throughput from about `114.2` to `159.7 eval/s`, and mean readback sync fell from about `25.96 ms` to `16.37 ms`. After cleanup, no `agent-browser`, Chrome, or Vite `5179` processes remained. This is a lifecycle/process-cleanup smoke, not a browser-memory leak proof.
  - Added `npm run lc0:browser-hybrid-search-fixture-parity`, a browser/WebGPU fixed-search parity harness that runs native BLAS search fixture sets at selected visits and compares each `batchPipelineDepth` against both the native best move and the depth-1 browser baseline. The script defaults to parity-safe depth `1` and fails on mismatches; pass explicit exploratory depths such as `--batch-pipeline-depths 1,2,4 --allow-mismatches` only for artifact capture. Artifact `/tmp/lc0_hybrid_search_fixture_parity_wgsl_depths_32_64_128_r1.json` covered WGSL heads, hand encoder, `batch=4`, visits `32,64,128`, depths `1,2,4`, all 16 fixtures. Depth 1 matched native on `48/48`; depth 2 matched `46/48`; depth 4 matched `45/48`; overall `139/144` cells matched native/depth baseline. Mismatches were low-visit, marginal search-order cases (`in-check-king-evasion` at 32 visits and `italian-four-ply-history` at 32/64 visits). Conclusion: pipelined search is a useful throughput/readback-overlap experiment, but it is not fixed-search-parity safe enough for promotion; keep `batchPipelineDepth>1` benchmark-only and consider depth 2 only as a speculative analysis mode unless a parity-preserving scheduler is designed.
  - Longer deferred-readback lifecycle artifacts over all 16 representative fixtures: `/tmp/lc0_wgsl_deferred_readback_lifecycle_batch4_iters20_fixture16_r2.json` matched immediate/deferred best moves and improved throughput from about `114.5` to `161.4 eval/s` with mean readback sync `26.03 ms → 15.55 ms`; `/tmp/lc0_wgsl_deferred_readback_lifecycle_batch4_iters50_fixture16.json` improved throughput similarly (`114.1 → 158.5 eval/s`, readback `25.99 ms → 15.68 ms`) but had one deferred best-move mismatch among 200 evaluated positions (`black-to-move-after-e4`, `e7e5 → c7c5`). Treat this as an intermittent deferred-readback correctness/lifecycle warning: the overlap path needs repeated long-run matching or more result-buffer sequencing assertions before any use outside benchmarks.
  - Follow-up sequencing hardening pass: WGSL-head submit/finish records now carry monotonic `wgslSequenceId`, `batchSequenceIndex`, and `deferredReadbackSlot` diagnostics; deferred readback buffers are tracked as in-use and throw if a ring slot is reused before its previous result is mapped. Timing is also split into `readbackMapAsyncMs`, `readbackMapCopyMs`, and `deferredReadbackDelayMs` to separate queue/map wait from CPU copy and attempted overlap. Pipelined PUCT now assigns generation IDs to leaf batches, rejects malformed `evaluateBatchSequence()` result shapes, and throws if a pending leaf is already expanded before its result is applied. Local smoke commands `--iters 1/2 --warmup 0 --batch 2 --fixture-limit 2` matched best moves; the two-batch smoke reported deferred `deferredReadbackDelayMs≈9.81 ms`, confirming the new overlap-delay attribution is visible.
  - Added `npm run lc0:browser-wgsl-lifecycle-smoke`, a browser harness for true repeated WGSL-head runtime lifecycle checks. Each cycle asks the worker to create a fresh hybrid runtime, exercise physical WGSL batches plus deferred double-buffer readback so the persistent WGSL upload/readback buffers are allocated, destroy the runtime, and record browser memory samples when Chromium exposes them. The script fails on any immediate/deferred best-move mismatch unless `--allow-mismatches` is passed for artifact capture.
- WGSL attention kernel fusion prototype:
  - Added an experimental `attentionFusion=score-softmax` attention-block path that fuses QK score calculation, smolgen-bias addition, and row softmax into one WGSL dispatch, reducing the isolated attention block from four dispatches per iteration (QKV, score, softmax, value) to three. This is wired only through the attention-block benchmark/probe path; the hybrid evaluator and stable ORT-head backend are unchanged.
  - Local browser smoke artifact: `/tmp/lc0_wgsl_attention_fusion_task9.json` from `/lc0-policy-only.html?attentionBlockBench=1&attentionBlockIters=1&attentionBlockWarmup=0&attentionFusion=score-softmax&packVerify=0&ep=wasm`. The fused path passed parity with `maxAbsError≈1.25e-6` and `rmsError≈1.61e-7`.
  - Performance result is not favorable yet: this first fused kernel uses one serial workgroup per attention row to preserve correctness, and the smoke run was much slower than the unfused path despite one fewer dispatch. Keep it as a correctness scaffold for a parallel row-softmax fusion, not as a promotion candidate.
- Stable backend protection:
  - Added `tests/lc0_stable_backend_defaults.test.mjs` to pin `Lc0WebHybridEvaluator` defaults to the stable `headBackend='ort'`, `inputBackend='js'`, `legalPriorsBackend='js'`, and explicit opt-in for experimental WGSL/WASM/GPU paths. This guards the default `lc0web-wgsl-encoder-ort-heads` route while the WGSL-heads, WASM input, WASM/GPU legal-prior, deferred-readback, and attention-fusion prototypes remain benchmark-only.
- Final promotion/closure pass, 2026-06-04:
  - Stable validation passed with `npm run typecheck`, `node --experimental-strip-types --test tests/lc0_stable_backend_defaults.test.mjs`, and `npm run lc0:browser-ci-smoke -- --fixture-limit 16 --out /tmp/lc0_final_ci_smoke_fixtures16.json --timeout 180000`. The CI smoke matched WGSL heads vs stable ORT heads on `16/16` representative fixtures and ended with `LC0_BROWSER_CI_SMOKE_LEAK_CHECK_CLEAN`.
  - Repeated local search matrix artifact: `/tmp/lc0_final_search_matrix_local.json` from `visits=1,32`, `batch=1,4`, backends `ort,wgsl`, 2 timed eval/search samples after warmup. Representative `32v/b4`: stable ORT heads `65.02 visits/s`, warm eval `19.46 ms`, search `492.20 ms`; experimental WGSL heads `109.74 visits/s`, warm eval `13.96 ms`, search `291.59 ms`. Both returned `d2d4` and completed requested visits.
  - Mac mini search matrix artifact: `/tmp/lc0_final_search_matrix_macmini.json` from the same shape with one timed sample after warmup. Representative `32v/b4`: stable ORT heads `92.62 visits/s`, warm eval `16.34 ms`, search `345.49 ms`; experimental WGSL heads `118.82 visits/s`, warm eval `15.03 ms`, search `269.32 ms`. Both returned `d2d4` and completed requested visits.
  - Final local WGSL physical-batch artifact: `/tmp/lc0_final_wgsl_batch_bench_local.json`. At `32v/b4`, serial WGSL batch search reported `56.64 visits/s`, 4 readback maps, and batch-eval readback `42.54 ms`; physical WGSL batch reported `107.64 visits/s`, 1 readback map, physical batch size 4, and batch-eval readback `31.56 ms`. This supports keeping physical batching as the experimental WGSL-head default, while stable ORT-heads remain the production default.
  - Final local JS-vs-SIMD-WASM input artifact: `/tmp/lc0_final_input_bench_local.json` over all 16 fixtures with 2 timed iterations after warmup. JS input averaged total eval `15.65 ms` and input build `2.61 ms`; WASM input averaged total eval `10.31 ms`, input build `0.052 ms`, bridge/copy `0.029 ms`, wasm encode `0.009 ms`, and wasm total `0.040 ms`. All fixture best moves matched JS. This run is encouraging, but the path still stays opt-in until repeated fixture parity and search-level wins are stable across hosts.
  - Final deferred-readback artifacts: `/tmp/lc0_final_deferred_readback_local.json` and `/tmp/lc0_final_deferred_readback_macmini.json`. Both matched best moves. Local immediate vs deferred was `113.61` vs `151.87 eval/s` with readback mean `25.99` vs `17.33 ms`; Mac mini immediate vs deferred was `120.75` vs `157.60 eval/s` with readback mean `24.78` vs `16.62 ms`. This is real overlap evidence for independent batch streams, but it remains benchmark-only because the live PUCT loop still consumes priors synchronously.
  - Final attention-fusion comparison artifact: `/tmp/lc0_final_attention_fusion_local.json`. The fused score+softmax route preserved parity (`maxAbsError≈1.25e-6`) and reduced dispatches from 4 to 3, but synchronized per-dispatch estimate was worse (`2.41 ms` fused vs `0.303 ms` unfused) because the current fused kernel serializes each row. Keep it as a correctness scaffold for a parallel fused row-softmax, not a promotion candidate.
  - Mac mini JS-vs-WASM input benchmark over all fixtures timed out twice before `HYBRID_INPUT_BENCH_DONE` (240s and 480s browser waits) after syncing the tree; local input evidence is therefore not enough for promotion. Mac mini search/deferred-readback coverage did complete.
  - Promotion decision after the pass: keep `lc0web-wgsl-encoder-ort-heads` as the stable default. Keep WGSL heads, WASM input/legal priors, deferred readback, and attention fusion behind explicit benchmark/experimental opt-ins until they satisfy full fixture parity, repeated Chromium/WebGPU runs, no fallback surprises, clean leak checks, and search-level latency wins on both local and Mac mini hosts.
- Autoresearch WASM-input/mixed-kernel lane, 2026-06-05:
  - The lane now uses a scoped benchmark-hygiene reset before each `autoresearch.sh` run: kill wrapper-owned `agent-browser-darwin-arm64`, `.agent-browser` Chrome-for-Testing processes, and default Vite listeners on `5179/5180`, then wait before measuring. This was added after a degraded browser/WebGPU window inflated mixed-TVM JS-legal controls to about `10.3–11.7 ms/eval` and ONNX b1 to `17.7 ms/eval`; a hard reset recovered mixed-TVM JS-legal to about `6.1–6.2 ms/eval`. Treat this as measurement hygiene, not a runtime optimization.
  - Recovered-state adjacent controls favored the opt-in path `hybrid-wgsl-heads`, WASM input, `encoderKernel=mixed-tvm-ffn`, JS legal priors, `lc0BatchSize=4`, `batchPipelineDepth=1`: two 16-position/3-rep controls reported `6.11` and `6.18 ms/eval`, and a 32-position/3-rep confirmation reported `6.11 ms/eval` with readback wait around `5.8 ms`.
  - Adjacent recovered controls were slower for hand WGSL (`7.14 ms/eval`), mixed-TVM with GPU legal priors (`6.58 ms/eval` despite `3084` readback bytes and one extra dispatch), and ONNX b1 (`10.66 ms/eval`). This supports productizing/documenting the mixed-TVM JS-legal path as an explicit opt-in candidate, while keeping ORT ONNX/WebGPU as the stable default and keeping GPU legal priors, pipe2/depth>1, and readback micro-toggles out of the primary path.
  - Recommended next research lane update: a simple WebGPU int8 FFN prototype passed drift but regressed throughput, so GPU work pivoted to smolgen f16 tiling/generation. The first keep is `encoderKernel=mixed-tvm-ffn-smolgen-project`, which keeps mixed-TVM FFN and uses a 64-column tiled-f16 smolgen-project kernel; it reconfirmed at `5.416 ms/eval` on the recovered-state mixed-TVM JS-legal 32-position fixed suite with 9-fixture drift passing. A simple tiled smolgen-dense1 variant and combined project+dense1 variant passed drift but failed to beat project-only (`5.57 ms/eval` combined vs `5.46 ms/eval` same-session project-only), so those dense1 shapes were reverted. Project tile-size sweep variants at 32 and 128 columns did not improve on 64 columns. The first attempt to fuse smolgen project into attention-score failed WGSL-head drift with zero/uniform mapped policy and was reverted; future fusion needs isolated score/bias parity first. Adjacent toggles also lost: project+TVM outproj (`7.11 ms/eval` fast), project+TVM QKV (`8.05 ms/eval` fast), GPU/WASM legal priors (`6.37`/`6.38 ms/eval` fast), and JS/WGSL input (`8.78`/`6.48 ms/eval` fast). A temporary dense1+swish+LN1 fusion reduced dispatches but did not improve throughput (`5.47 ms/eval` default confirmation) and increased top-prior drift, so it was reverted. CPU/WASM int8 remains a separate possible fallback lane.
- Batch-fill audit:
  - Added `evalBatchSizeHistogram` and derived average batch-size summaries to search stats/matrix outputs so the browser harness can distinguish full leaf batches from singleton-heavy collection.
  - Local Chromium/WebGPU artifact: `/tmp/lc0_batch_fill_audit_local.json`. Matrix shape: backends `ort,wgsl`, visits `128`, batches `1,2,4,8`, `reuseTree=0`, `evalCacheEntries=0`, one timed search per cell, no warmup, `ep=wasm`, pack verification disabled. All cells returned `d2d4`, completed `128/128` visits, and stopped by `visit-budget`.
  - Batch collection is not mostly singleton-limited in this fixed-root workload: batch `2` produced histogram `{2:64}`, batch `4` produced `{4:32}`, and batch `8` produced `{8:16}` for both ORT-heads and WGSL-heads. That means the current leaf collection/virtual-visit retry path fills requested physical batches cleanly; the observed batch-size throughput differences are more likely backend/runtime synchronization or per-eval overhead tradeoffs than failure to fill leaf batches, so no batching-threshold tuning was applied in this pass.
- CI-style browser smoke harness:
  - Added `npm run lc0:browser-ci-smoke`, which starts one Vite server unless `--base-url` is supplied, runs stable hybrid ORT-heads search, experimental hybrid WGSL-heads search, mapped-policy probe, WGSL-heads-vs-ORT fixtures, closes browser sessions, kills leftover Chrome/agent-browser processes, and fails if the final leak scan is not clean.
  - Local artifact after review fixes: `/tmp/lc0_browser_ci_smoke_task4_reviewfix.json` from `npm run lc0:browser-ci-smoke -- --fixture-limit 1 --timeout 180000`. It passed all four smoke rows and ended with `LC0_BROWSER_CI_SMOKE_LEAK_CHECK_CLEAN`; review follow-up also verified strict Vite port failure on an occupied `5179` and fail-fast probe validation with `--max-error 1e-12`.
- Representative parity fixtures:
  - Expanded FEN-only fixtures from 6 to 10 positions by adding black-to-move castling, black en-passant, black promotion, and in-check king-evasion coverage alongside startpos/opening, white castling/en-passant/promotion, and rule50 fixtures.
  - Expanded explicit-history fixtures from 3 to 6 histories by adding a longer Queen's Gambit castled line, a capture/queen-retreat line, and a black en-passant history. Regenerated BLAS native nodes=1 priors plus nodes `32/64/128` search artifacts for the full fixture set and copied the browser-facing priors/search32 JSONL files under `public/lc0/`.
  - Validation passed for fixture parsing, f32 ONNX-vs-native BLAS prior parity, PUCT native search parity at `32/64/128` visits, and browser WGSL-heads-vs-ORT parity on all 16 representative fixtures. Local browser artifact: `/tmp/lc0_browser_ci_smoke_fixtures16.json`; WGSL heads matched ORT heads on `16/16` best moves, with max mapped-policy diff about `4.53e-6`, max WDL diff about `1.79e-7`, and a clean leak check.
- Pre-WGSL-heads local + Mac mini baseline matrix for `lc0web-wgsl-encoder-ort-heads`:
  - Drift parity, local Chromium 149: `npm run lc0:browser-hybrid-drift -- --limit 9 --timeout 300000 --baseline-mode serial` and `--limit 16` both completed. The available fixture set currently contains 9 fixtures, so the `--limit 16` run also evaluated 9. Best moves matched f32 ONNX and native BLAS on `9/9`; local max drift ranges across the two runs were f32 WDL `0.00113–0.00205`, native WDL `0.00239–0.00283`, f32 top-prior `0.00172–0.00349`, native top-prior `0.00128–0.00324`.
  - Drift parity, Mac mini Chromium 148: the same `--limit 9` and `--limit 16` serial runs completed, again evaluating the 9 available fixtures. Best moves matched f32 ONNX and native BLAS on `9/9`; max drift ranges were f32 WDL `0.000156–0.000315`, native WDL `0.00189`, f32 top-prior `0.000737–0.00271`, native top-prior `0.000824–0.00238`.
  - Search latency, local Chromium 149 with `requestedEp=wasm`, `packVerification=disabled`, batch `1`, 3 timed iterations per row: visits `1` => warm eval mean `41.43 ms`, search mean `86.93 ms`, `11.50 visits/s`; visits `32` => warm eval mean `42.77 ms`, search mean `1351.37 ms`, `23.68 visits/s`; visits `128` => warm eval mean `42.30 ms`, search mean `5314.93 ms`, `24.08 visits/s`. All rows returned best move `d2d4`; search stats showed no cache hits within a fresh search and neural misses of visits+1.
  - Search latency, Mac mini Chromium 148 with `requestedEp=wasm`, `packVerification=disabled`, batch `1`, 3 timed iterations per row: visits `1` => warm eval mean `37.23 ms`, search mean `73.13 ms`, `13.67 visits/s`; visits `32` => warm eval mean `40.17 ms`, search mean `1441.93 ms`, `22.19 visits/s`; visits `128` => warm eval mean `37.23 ms`, search mean `4607.00 ms`, `27.78 visits/s`. All rows returned best move `d2d4`; search stats showed no cache hits within a fresh search and neural misses of visits+1.
  - Cleanup check after the repeated matrix found no local or remote Chrome for Testing, `agent-browser`, Vite, or lc0 browser Node processes left. This remains a baseline/bottleneck measurement, not promotion evidence.

Validation commands used during this checkpoint:

```sh
npm run typecheck
TINY_LEELA_ORT_EP=wasm node --experimental-strip-types --test tests/lc0_wgsl_kernel_probe.test.mjs
npm run lc0:browser-wgsl-smokes -- --no-server --only encoder0-ffn,encoder0-block --timeout 25000
npm run lc0:browser-wgsl-smokes -- --no-server --only attention-value-ort-wasm --timeout 25000
npm run lc0:browser-wgsl-smokes -- --only attention-output-ort-wasm --timeout 25000
npm run lc0:browser-wgsl-smokes -- --only encoder0-ffn-ort-wasm --timeout 25000
npm run lc0:browser-wgsl-smokes -- --only encoder0-block-ort-wasm --timeout 25000
npm run lc0:browser-wgsl-smokes -- --only encoder1-block --timeout 25000
npm run lc0:browser-wgsl-smokes -- --only encoder-stack-2-wasm --timeout 25000
npm run lc0:browser-wgsl-smokes -- --no-server --only attention-output,encoder0-ffn --timeout 50000
npm run lc0:browser-wgsl-smokes -- --no-server --only encoder0-block,encoder-stack-2-wasm --timeout 50000
npm run lc0:browser-wgsl-smokes -- --no-server --only attention-output,encoder0-block --timeout 50000
npm run lc0:browser-wgsl-smokes -- --no-server --only encoder-stack-2-wasm,encoder-stack-heads-2-wasm --timeout 80000
npm run lc0:browser-wgsl-smokes -- --no-server --only encoder-stack-10-wasm --timeout 80000
npm run lc0:browser-wgsl-smokes -- --no-server --only encoder-stack-heads-2-wasm --timeout 120000
# Manual browser routes:
#   /lc0-policy-only.html?mappedPolicyProbe=1
#   /lc0-policy-only.html?wgslHeadsProbe=1&ep=wasm&packVerify=0
#   /lc0-policy-only.html?wgslHeadsVsOrt=1&fixtureLimit=9&encoderLayers=10&ep=wasm&packVerify=0
npm run lc0:browser-hybrid-drift -- --no-server --limit 3 --timeout 180000
npm run lc0:browser-hybrid-drift -- --limit 9 --timeout 300000
npm run lc0:browser-hybrid-drift -- --limit 9 --timeout 300000 --baseline-mode serial
npm run lc0:browser-hybrid-drift -- --limit 16 --timeout 300000 --baseline-mode serial
npm run lc0:browser-hybrid-drift -- --head-backend wgsl --limit 9 --timeout 300000 --baseline-mode serial
npm run lc0:browser-hybrid-drift -- --head-backend wgsl --limit 16 --timeout 300000 --baseline-mode serial
npm run lc0:browser-hybrid-search-bench -- --dry-run --visits 8 --eval-iters 1 --search-iters 1
npm run lc0:browser-hybrid-search-bench -- --visits 1 --eval-iters 3 --eval-warmup 1 --search-iters 3 --search-warmup 1 --timeout 300000
npm run lc0:browser-hybrid-search-bench -- --visits 32 --eval-iters 3 --eval-warmup 1 --search-iters 3 --search-warmup 1 --timeout 300000
npm run lc0:browser-hybrid-search-bench -- --visits 128 --eval-iters 3 --eval-warmup 1 --search-iters 3 --search-warmup 1 --timeout 300000
npm run lc0:browser-hybrid-search-bench -- --head-backend wgsl --visits 1 --eval-iters 3 --eval-warmup 1 --search-iters 3 --search-warmup 1 --timeout 300000
npm run lc0:browser-hybrid-search-bench -- --head-backend wgsl --visits 32 --eval-iters 3 --eval-warmup 1 --search-iters 3 --search-warmup 1 --timeout 300000
npm run lc0:browser-hybrid-search-bench -- --head-backend wgsl --visits 128 --eval-iters 3 --eval-warmup 1 --search-iters 3 --search-warmup 1 --timeout 300000
npm run lc0:browser-hybrid-search-matrix -- --out /tmp/lc0_postmerge_search_matrix_local.json --timeout 240000
node --experimental-strip-types --test tests/lc0_fen_fixtures.test.mjs tests/lc0_history_fixtures.test.mjs tests/lc0_onnx_native_fixture_parity.test.mjs tests/lc0_onnx_history_parity.test.mjs tests/lc0_search.test.mjs
npm run lc0:browser-ci-smoke -- --out /tmp/lc0_browser_ci_smoke_fixtures16.json --fixture-limit 16 --timeout 300000
# Mac mini: rsync current leelaweb checkout, then:
#   npm run lc0:browser-hybrid-search-matrix -- --out /tmp/lc0_postmerge_search_matrix_remote.json --timeout 240000
npm run lc0:browser-wgsl-vs-ort-webgpu -- --dry-run --samples 2
npm run lc0:browser-wgsl-vs-ort-webgpu -- --samples 2 --timeout 25000 --wgsl-iters 1 --ort-iters 2
npm run lc0:browser-wgsl-vs-ort-webgpu -- --samples 10 --timeout 25000 --wgsl-iters 3 --ort-iters 3
npm run lc0:browser-wgsl-vs-ort-webgpu -- --samples 4 --timeout 60000 --wgsl-iters 3 --ort-iters 3
```

## Current interpretation

The custom path now validates a complete encoder0 block in staged WGSL form, including smolgen score bias and FFN. This is a stronger milestone than the earlier attention-core-only checkpoint, but it is still not an end-to-end LC0 evaluator:

- The reusable encoder-block loop now covers `/encoder0` → `/encoder9` with GPU-buffer handoff. The browser worker can now run a hybrid evaluator path that builds real LC0 112-plane inputs, feeds the full WGSL encoder stack, and uses tiny f32 ONNX/ORT mapped-policy + WDL heads. Persistent evaluator instances now reuse loaded pack tensor views, encoder GPU buffers/bind groups/pipelines, and the ORT head session across evaluations. The hybrid evaluator now computes smolgen bias on GPU and removes the per-layer CPU readback; it performs one final encoder-stack readback for the ORT heads. A 9-fixture browser drift sweep matched f32 ONNX and native BLAS best moves on all fixtures, with max WDL/top-prior drift vs f32 ONNX of about `0.00387`/`0.01094` before GPU smolgen and about `0.00161`/`0.00128` in a representative post-GPU-smolgen repeat.
- Timing reports both command submission/readback synchronization and, when Chromium exposes WebGPU `timestamp-query`, a GPU timestamp duration for the encoder0 attention+FFN command sequence.
- The full encoder0 benchmark no longer forces an explicit queue-completion boundary between attention-output and FFN; both command buffers are submitted together and rely on WebGPU queue ordering for the ln1-output → FFN-dense1 dependency.
- The per-stage encoder0 timing breakdown now covers tiled projection/FFN kernels and the parallel ln1/ln2 reduction; stage timings still include queue-completion overhead and remain bottleneck hints rather than promotion evidence.
- ORT tiny comparisons are same-value subgraph checks, not full deployment performance proof.

## TVM/WebGPU fixed-shape feasibility spike, 2026-06-04

A bounded local Apache TVM source-build spike generated browser-valid WGSL for three LC0-like hot shapes before attempting any whole-model lowering. The working setup lives outside the repo under `/tmp/tvm-webgpu-src` with Python env `/tmp/lc0-tvm-spike-py311`; importing TVM requires `DYLD_LIBRARY_PATH=/tmp/tvm-webgpu-src/build/lib` and `PYTHONPATH=/tmp/tvm-webgpu-src/python`. Yukon is reachable and has an RTX 3090, but it did not have TVM/PyTorch or `nvcc` available during the spike, so local Chromium/WebGPU stayed the lower-friction path for browser conclusions.

Generated fixed-shape artifacts:

- `/tmp/lc0_tvm_wgsl_shapes/proj_64x256x256.{matmul_kernel,add_kernel}.wgsl`
- `/tmp/lc0_tvm_wgsl_shapes/ffn_dense1_64x256x1024.{matmul_kernel,add_kernel}.wgsl`
- `/tmp/lc0_tvm_wgsl_shapes/ffn_dense2_64x1024x256.{matmul_kernel,add_kernel}.wgsl`
- `/tmp/lc0_tvm_wgsl_shapes/ffn_dense1_64x256x1024.matmul_kernel.patched_sqrrelu.wgsl`
- `/tmp/lc0_tvm_wgsl_shapes/ffn_dense2_64x1024x256.matmul_kernel.patched_residual.wgsl`
- `/tmp/lc0_tvm_wgsl_shapes/ffn_dense1_64x256x1024.matmul_kernel.patched_sqrrelu_f16_lc0layout.wgsl`
- `/tmp/lc0_tvm_wgsl_shapes/ffn_dense2_64x1024x256.matmul_kernel.patched_residual_f16_lc0layout.wgsl`
- `/tmp/lc0_tvm_webgpu_spike_report.md`

The per-kernel WGSL chunks compiled cleanly in Chromium/WebGPU with zero shader compilation errors or warnings. The aggregate TVM source dump is not directly loadable as one shader because TVM repeats per-kernel `PODArgs`/`podArgs`; use the split kernel chunks.

Initial browser microbench artifact: `/tmp/lc0_tvm_vs_hand_bench_result.json`. This compared TVM's generated f32 `matmul_kernel + add_kernel` against a simple LC0-style one-dispatch f32 tiled matmul+bias kernel on the same shapes. TVM's tiled schedule was faster even with two dispatches: `64x256x256` was `0.054 ms` vs hand `0.078 ms` (`1.44x`), `64x256x1024` was `0.0735 ms` vs hand `0.198 ms` (`2.69x`), and `64x1024x256` was `0.135 ms` vs hand `0.1935 ms` (`1.43x`). Errors vs hand f32 accumulation were about `8e-8` to `2.6e-7` max absolute.

Patched dense1 epilogue experiment artifact: `/tmp/lc0_tvm_fused_epilogue_bench_result.json`. The TVM `ffn_dense1_64x256x1024` matmul kernel epilogue was manually patched to add bias and apply sqrReLU before the final store, eliminating the separate epilogue dispatch while preserving TVM's tiled matmul schedule. The patched shader compiled cleanly and matched the hand fused f32 kernel with max absolute error `1.26e-8`. Timings: hand fused `0.181 ms` avg, TVM split matmul+manual sqrReLU epilogue `0.0643 ms`, patched TVM fused epilogue `0.0566 ms`. The patched TVM kernel was `3.20x` faster than the hand fused f32 kernel and `1.14x` faster than the split TVM path in this local browser run.

Patched dense2 residual experiment artifact: `/tmp/lc0_tvm_dense2_residual_bench_result.json`. The TVM `ffn_dense2_64x1024x256` matmul kernel epilogue was patched to add bias, multiply by FFN alpha, and add the residual input before the final store. It compiled cleanly and matched the hand fused f32 dense2+residual kernel with max absolute error `1.84e-7`. Timings: hand fused dense2+residual `0.189 ms` avg, TVM split matmul+manual residual epilogue `0.111 ms`, patched TVM fused epilogue `0.111 ms`. The fused patch was `1.70x` faster than the hand kernel but effectively tied with the split TVM path in this noisy local run, so dense2's epilogue fusion mainly removes a dispatch rather than producing an isolated kernel-time win.

Full f32 FFN chain artifact: `/tmp/lc0_tvm_full_ffn_bench_result.json`. This compared the two-dispatch hand f32 FFN path, a four-dispatch TVM split path (`dense1 matmul`, sqrReLU epilogue, `dense2 matmul`, residual epilogue), and the two-dispatch patched TVM path (`dense1+sqrReLU`, `dense2+residual`). The patched TVM FFN matched hand output with max absolute error `6.71e-8`. Timings: hand FFN `0.372 ms` avg, TVM split `0.190 ms`, patched TVM two-dispatch FFN `0.178 ms`; patched TVM was `2.09x` faster than hand and `1.07x` faster than the four-dispatch TVM split path.

Packed-f16 LC0-layout FFN artifact: `/tmp/lc0_tvm_full_ffn_f16_lc0layout_bench_result.json`. The patched TVM dense kernels were adapted from f32 row-major weights to the current LC0-style packed f16 transposed weight layout (`output_col * input_dim + input_k` packed two fp16 lanes per `u32`) and f16 bias/alpha buffers. The two-dispatch TVM f16 FFN matched the hand f16 LC0-style FFN with max absolute error `7.36e-8`. Timings: hand f16 FFN `0.261 ms` avg, patched TVM f16 FFN `0.112 ms` avg; TVM was `2.33x` faster than the hand f16 FFN in this local Chromium/WebGPU run. This is the first spike result aligned with the deployed pack's packed-f16 weight constraint rather than only raw f32 buffers.

Packed-f16 LC0-layout FFN+LN2 artifact: `/tmp/lc0_tvm_full_ffn_ln2_f16_lc0layout_bench_result.json`. This kept the same patched TVM dense1/dense2 kernels, added the existing one-dispatch 256-channel layernorm shape after residual, and compared against the hand LC0-style f16 FFN+LN2 chain. The TVM chain matched the hand chain with max absolute error `1.91e-6` and RMS error `3.60e-7`, consistent with layernorm's amplified tiny input differences. Timings: hand f16 FFN+LN2 `0.342 ms` avg, patched TVM f16 FFN+LN2 `0.145 ms` avg; TVM was `2.37x` faster. Layernorm does not erase the dense-kernel win, so the next real integration target is replacing the current experimental FFN dense kernels while leaving LN2 unchanged.

Repo integration smoke artifact: `/tmp/lc0_tvm_integrated_ffn_bench_result.json`. The patched TVM packed-f16 dense1/dense2 WGSL was embedded behind an opt-in FFN benchmark variant, selectable with `encoder0FfnKernel=tvm-packed-f16` / `ffnKernel=tvm-packed-f16`; the default remains the existing hand FFN kernels. Validation command: `npm run lc0:browser-wgsl-smokes -- --only encoder0-ffn,encoder0-ffn-tvm-packed-f16 --timeout 60000`, which passed both variants. A local Chromium/WebGPU 20-iteration route comparison on the real batch-8 f16 lc0web pack produced max errors `2.38e-6` for hand and `2.86e-6` for TVM-packed-f16; estimated per-dispatch synced time was `0.325 ms` for hand and `0.2105 ms` for TVM (`1.54x`). Treat this as an integration/smoke signal, not final promotion evidence, because it is a single queued-readback estimate.

Attention QKV integration artifact: `/tmp/lc0_tvm_attention_qkv_integration_bench_result.json`. The TVM-style `64x256x256` projection schedule was adapted to the same packed-f16 transposed LC0 weight layout and f16 bias buffers, then wired behind an opt-in attention-block QKV variant selectable with `attentionQkvKernel=tvm-packed-f16` / `attnQkvKernel=tvm-packed-f16`; default QKV remains the existing hand fused Q/K/V projection. This QKV variant uses three TVM projection dispatches (Q, K, V) feeding the existing score, smolgen-bias add, softmax, and value kernels. Validation command: `npm run lc0:browser-wgsl-smokes -- --only attention-block,attention-block-tvm-qkv-packed-f16 --timeout 60000`, which passed both variants. A local Chromium/WebGPU 20-iteration route comparison on the real batch-8 f16 pack produced max errors `1.25e-6` for hand and `1.19e-6` for TVM-packed-f16 QKV; estimated per-iteration synced time was `0.317 ms` for hand and `0.170 ms` for the TVM QKV attention block (`1.87x`). Treat this as an integration/smoke signal: it improves the attention-block route despite two additional QKV dispatches, but it is not yet wired through all encoder layers or promoted.

Attention output-projection integration artifact: `/tmp/lc0_tvm_attention_outproj_integration_bench_result.json`. The same TVM-style `64x256x256` projection schedule was patched for the attention output dense epilogue (`bias * alpha + residual`) and wired behind an opt-in attention-output projection variant selectable with `attentionOutProjKernel=tvm-packed-f16` / `attnOutProjKernel=tvm-packed-f16`; default output projection remains the current hand residual kernel. Validation command: `npm run lc0:browser-wgsl-smokes -- --only attention-output,attention-output-tvm-proj-packed-f16 --timeout 60000`, which passed both variants. A local Chromium/WebGPU 20-iteration route comparison on the real batch-8 f16 pack matched the hand path within max error `1.37e-6`, but estimated per-iteration synced time regressed from `0.178 ms` to `0.399 ms` (`0.45x` vs hand). This is useful parity/integration evidence but not a promotion candidate as-is; keep it opt-in/research-only unless later combined scheduling/fusion changes reverse the end-to-end result.

TVM WGSL source-of-truth note: the patched TVM packed-f16 WGSL kernels are now kept as source files under `src/lc0/kernels/tvm/` and projected into `src/lc0/generated/tvmPackedF16Wgsl.ts` by `npm run lc0:generate-tvm-wgsl`. The runtime imports the generated TS constants so Node-based tests still work without Vite raw-file imports, but the large WGSL bodies no longer live as hand-pasted constants in `wgslMatmulAddProbe.ts`.

Full hybrid encoder opt-in artifact: `/tmp/lc0_tvm_hybrid_encoder_kernel_integration_bench_result.json`. The hybrid evaluator now accepts `encoderKernel=tvm-packed-f16` / `hybridEncoderKernel=tvm-packed-f16` / `encoderKernelVariant=tvm-packed-f16`, which wires all 10 WGSL encoder layers through the TVM packed-f16 QKV projections, TVM packed-f16 attention output projection/residual, and TVM packed-f16 FFN dense kernels while preserving the existing smolgen, attention score/softmax/value, layernorm, ORT-head path, and stable default `hand` encoder kernels. The smoke harness `scripts/lc0_browser_hybrid_search_bench.mjs` also accepts `--encoder-kernel tvm-packed-f16` and verifies the reported encoder variant. A local Chromium/WebGPU 1-eval/1-visit route comparison preserved the same best move (`d2d4`) for both hand and TVM encoder variants, but the combined TVM encoder lane was slower in that tiny route sample: eval mean `619.2 ms` vs hand `403.2 ms`, encoder readback-synced phase `187.1 ms` vs `50.3 ms`, and backend total-eval timing `206.3 ms` vs `70.4 ms` (`0.65x` eval speedup vs hand). A later warmed 10-eval/3-search browser rerun was much closer (`/tmp/lc0_hybrid_search_hand_profile_context_10.json`, `/tmp/lc0_hybrid_search_tvm_profile_context_10.json`): eval mean `16.96 ms` hand vs `17.50 ms` TVM, readback-synced mean `12.95 ms` hand vs `13.65 ms` TVM, and search mean `36.69 ms` hand vs `32.32 ms` TVM. This suggests the original huge full-encoder regression was largely cold/browser noise and that the warmed full encoder is roughly parity, still not a promotion win. A separate two-fixture browser drift smoke (`/tmp/lc0_tvm_hybrid_encoder_kernel_drift_smoke.json`) also completed with `encoderKernelVariant: "tvm-packed-f16"` and plausible best moves (`d2d4`, `e7e5`). This proves full-stack opt-in wiring and basic browser viability, not promotion readiness.

Hybrid encoder profiling route: `lc0-policy-only.html?hybridEncoderProfile=1` now supports both sync-staged and lower-perturbation GPU timestamp attribution through all hybrid encoder layers; it accepts the same `encoderKernel`, `inputBackend`, `encoderLayers`, and `packVerify` knobs plus `hybridEncoderProfileIters` / `hybridEncoderProfileWarmup` and `hybridEncoderProfileMode=gpu-timestamp|sync-staged`. Sync-staged artifacts: `/tmp/lc0_hybrid_encoder_profile_hand_iters3.json`, `/tmp/lc0_hybrid_encoder_profile_tvm_iters3.json`, and summary `/tmp/lc0_hybrid_encoder_profile_summary.json`. GPU timestamp artifacts: `/tmp/lc0_hybrid_encoder_profile_hand_timestamp_iters10.json`, `/tmp/lc0_hybrid_encoder_profile_tvm_timestamp_iters10.json`, and summary `/tmp/lc0_hybrid_encoder_profile_timestamp_summary_iters10.json`. The timestamp path encodes the full encoder as one command buffer per iteration, inserts timestamped pass boundaries around stages, and reads timestamp queries once per iteration; on local Chromium/WebGPU timestamp queries were available. The warmed 10-iteration timestamp comparison reported profiled stage totals of `80.41 ms` hand vs `94.37 ms` TVM (`1.17x` TVM/hand). TVM won FFN dense1 (`0.054 ms` vs hand `0.125 ms`) and FFN dense2/residual (`0.113 ms` vs `0.147 ms`) in this profile, while QKV was slower (`0.112 ms` vs `0.066 ms`) and unchanged smolgen still appeared slower (`0.553 ms` vs `0.364 ms`), so these figures are useful attribution but still include pass-boundary/timestamp granularity effects rather than exact route latency.

Mixed TVM-FFN encoder opt-ins: `encoderKernel=mixed-tvm-ffn` / `hybridEncoderKernel=mixed-tvm-ffn` / `encoderKernelVariant=mixed-tvm-ffn` keeps hand QKV, hand attention output projection/residual, hand smolgen, hand attention score/softmax/value, and hand layernorms while using the TVM packed-f16 FFN dense1 and dense2/residual kernels across all 10 encoder layers. `encoderKernel=mixed-tvm-ffn-outproj` additionally A/Bs the TVM packed-f16 attention output projection/residual while still keeping hand QKV. The search and drift harnesses accept `--encoder-kernel mixed-tvm-ffn` and `--encoder-kernel mixed-tvm-ffn-outproj` and verify the reported variant. Artifacts: `/tmp/lc0_hybrid_encoder_profile_mixed_tvm_ffn_timestamp_iters10.json`, `/tmp/lc0_hybrid_encoder_profile_mixed_tvm_ffn_timestamp_summary.json`, `/tmp/lc0_hybrid_search_mixed_tvm_ffn_10.json`, `/tmp/lc0_hybrid_search_hand_after_mixed_10.json`, `/tmp/lc0_hybrid_encoder_profile_mixed_tvm_ffn_outproj_timestamp_iters10.json`, `/tmp/lc0_hybrid_encoder_profile_mixed_tvm_ffn_outproj_timestamp_iters10_fixed_rerun.json`, `/tmp/lc0_hybrid_encoder_profile_mixed_tvm_ffn_outproj_timestamp_summary.json`, `/tmp/lc0_hybrid_search_mixed_tvm_ffn_outproj_fixed_10.json`, `/tmp/lc0_hybrid_search_hand_after_outproj_fixed_10.json`, and `/tmp/lc0_hybrid_drift_mixed_tvm_ffn_outproj_limit2_fixed.json`. The first local Chromium/WebGPU timestamp profile put mixed TVM-FFN between hand and full TVM (`84.21 ms` mixed vs `80.41 ms` hand and `94.37 ms` full TVM), with expected FFN wins but residual apparent overhead/noise in unchanged stages. After fixing the out-projection/QKV selector order, the warmed mixed TVM-FFN+outproj timestamp rerun was the best timestamp sample (`74.38 ms`, `0.93x` vs hand), with hand QKV retained (`0.077 ms` avg in the rerun) and output projection near hand (`0.043 ms` vs hand `0.045 ms`). Route-level warmed eval samples preserved best move `d2d4`; the fixed mixed TVM-FFN+outproj rerun reported `15.51 ms` eval mean vs a nearby hand rerun of `20.76 ms`, but route timing has been noisy enough that this needs repeated validation before any promotion claim. Repeated matrix harnesses now cover these variants: `scripts/lc0_browser_hybrid_search_matrix.mjs` accepts `--encoder-kernels` and `--repeats`, and `scripts/lc0_browser_hybrid_encoder_profile_matrix.mjs` profiles encoder stage timings by variant. The first alternating Chromium/WebGPU matrix artifacts are `/tmp/lc0_hybrid_encoder_profile_variant_matrix_r2.json` and `/tmp/lc0_hybrid_search_variant_matrix_r2.json`: profile means over two repeats were hand `90.05 ms`, mixed FFN `79.30 ms`, mixed FFN+outproj `124.68 ms`, and full TVM `137.59 ms`; route eval means were hand `17.68 ms`, mixed FFN `20.24 ms`, mixed FFN+outproj `16.66 ms`, and full TVM `18.50 ms`. That split result says output-projection remains context/noise sensitive: route eval still likes `mixed-tvm-ffn-outproj`, but timestamp attribution did not reproduce the earlier win. The fixed nine-fixture drift smoke `/tmp/lc0_hybrid_drift_mixed_tvm_ffn_outproj_limit9.json` matched f32 and native best moves for all 9 fixtures with small f32 WDL/prior drift. A one-cell lifecycle smoke `/tmp/lc0_hybrid_encoder_profile_lifecycle_smoke.json` left no agent-browser/Chrome processes after cleanup. These mixed lanes are useful A/B and validation scaffolds, not promotion candidates yet.

Interpretation: TVM's raw matmul schedule is useful and likely better than the current simple hand-written LC0 tiled kernels for several fixed dense shapes, but not every adapted projection wins in the live route. LC0-specific epilogue patching is feasible: dense1 benefits clearly from fusing bias+sqrReLU, while dense2+residual mostly benefits at the FFN-chain dispatch level. The packed-f16 LC0-layout, FFN+LN2, and opt-in repo-harness results remove the biggest immediate layout and layernorm-adjacency objections, but this is still not a drop-in runtime replacement because the live encoder stack also includes attention/smolgen stages, per-layer buffers, model-pack integration, and search/evaluator scheduling. The promising near-term direction is a TVM-inspired or TVM-generated kernel lane: preserve TVM's stronger tiling, generate LC0-specific fused epilogues and packed-f16 loads, then compare end-to-end encoder/search latency. WebLLM/MLC is a precedent for compiler-generated backend kernels and profitable op fusion, but it is not a single whole-model super-kernel; it uses many optimized WebGPU/WGSL kernels plus fusion where dispatch/intermediate-memory savings justify it. For LC0, promotion should depend on browser dispatch count, synchronization count, readback bytes, parity fixtures, and search-level latency, not only isolated matmul time.

## Arena/analysis opt-in exposure, 2026-06-04

The arena and analysis pages now expose an LC0 backend selector without changing stable defaults. `ORT ONNX` remains the default. The opt-in choices are `WGSL encoder + ORT heads` and `WGSL encoder + WGSL heads (experimental)`, backed by the existing `lc0web` pack URL (`pack`/`modelPack` query params still override it). URL shortcuts also work: `lc0Runtime=hybrid-ort-heads`, `lc0Runtime=hybrid-wgsl-heads`, `runtime=wgsl-heads`, or `headBackend=wgsl`. Analysis initializes these paths in the dedicated search worker; arena uses the same evaluator interface on the page and disables backend switching during an active tournament. These controls are meant for manual tryout/A-B use, not promotion of WGSL heads as the default.

## Performance optimization backlog

The next custom-runtime performance work should prioritize reducing CPU/GPU synchronization before doing more isolated kernel micro-tuning. After the 2026-06-04 closure pass, the first follow-up sprint should make deferred/double-buffered readback and search-level overlap parity-safe, because `mapAsync`/queue-drain time remains the dominant noisy cost even when readback bytes shrink. TVM/f16/fusion work remains valuable, but defer it until the synchronization baseline is less likely to mask or exaggerate kernel wins.

1. **Make deferred/readback overlap parity-safe.** The WGSL-head backend already reads back only `(1858 + 3) * 4 = 7444` bytes with one `mapAsync`, and the GPU legal-prior scaffold can shrink that to `3084` bytes, but queue/map synchronization remains noisy and dominant. Batch-native evaluation, double-buffered/ring-buffered readback buffers, deferred `mapAsync` awaiting, strict result sequencing, and scheduling the next GPU eval while CPU search consumes the previous result are the likely paths to hide this cost. Treat this as a search-scheduler/broker problem as much as a shader problem: allow multiple eval requests in flight only where parity-safe, then measure whether CPU PUCT bookkeeping overlaps GPU work instead of immediately blocking on every result. The current `batchPipelineDepth>1` path is benchmark-only because fixture parity is not stable enough yet.
2. **Extend true batched WGSL-head evaluation.** The experimental WGSL-head backend now has a physical batch path, but it still needs larger-visit repeat runs, wider host coverage, and follow-up work to reduce one-off setup/readback cost. Continue targeting one upload, one command buffer, and one combined result map per physical search batch. Keep static batch-1 ORT-head sessions non-concurrent.
3. **Finish moving the initial input-body projection off JS.** The experimental `inputBackend=wgsl` path covers positional concat, input projection, Mish, and mul/add gates after CPU 112-plane encoding. Repeat against cached JS over larger search workloads, then promote only if parity and latency hold across hosts.
4. **Experiment with a SIMD WASM pre-GPU bridge.** For CPU-bound work that should stay outside WebGPU, prototype a C++ or Rust SIMD WASM module with a minimal JS bridge: board/history state in, compact encoded planes or packed feature buffers out, then `queue.writeBuffer()` into WebGPU. Candidate tasks are LC0 history/112-plane encoding, FEN/history replay helpers, legal-move generation, policy-index/legal-mask construction, and possibly PUCT bookkeeping. The goal is `SIMD WASM → minimal JS bridge → WebGPU`, not a second heavyweight runtime that duplicates model sessions or copies large buffers through JS unnecessarily.
5. **Fuse WGSL kernels where parity-safe.** After batching/sync work, revisit attention fusion points: QK scale + smolgen bias + softmax, softmax + attention-value, smolgen dense/activation/norm chains, and remaining correctness-first matmul paths. Keep the f32 ONNX/native BLAS parity ladder in place for every fusion.
6. **Prototype compiler/autotuned kernel generation as a research lane.** Do not replace the hand-written runtime first. Start with an offline generator/autotuner for one or two hot, fixed LC0 shapes (for example `64×256×256` projections or FFN dense layers), emit WGSL variants, benchmark them with the existing browser smoke harness, and keep only generated kernels that pass the same f32 ONNX/native parity gates. Candidate techniques include parameterized WGSL templates, local shape-specialized search over tile sizes/workgroup shapes/vectorization, or a bounded TVM/MLC/TensorIR feasibility spike if browser WGSL export is practical.
7. **Prototype INT4 or INT8 packed-weight kernels with fused dequantization.** The current `lc0web` pack is raw f16; add a separate experimental packed pack format and fused dequant-matmul kernels before touching the default runtime. Gates: policy/WDL drift vs f16/f32, best-move parity on representative fixtures, artifact byte reduction, browser memory reduction, and no search-latency regression after dequant overhead.
8. **Track deeper WebLLM-style dispatch reduction as a promotion criterion.** For any new generated, fused, or quantized path, report GPU dispatch count, `queue.onSubmittedWorkDone`/`mapAsync` count, readback bytes, and command-encoding time in addition to warm eval/search latency. Prefer changes that reduce host/GPU synchronization, not just isolated kernel time. Include a GPU legal-prior/top-k experiment in this bucket: upload legal move indices, gather/mask/softmax/top-k on GPU, and compare reading back only legal priors plus WDL against the current full 1858-logit readback.

Remaining CPU work by backend:

- Both hybrid backends: FEN/history reconstruction, LC0 112-plane encoding, current JS input-body projection, legal move generation, legal-prior softmax/sorting, PUCT tree/search bookkeeping, and CPU-side command encoding/readback waiting.
- Stable `lc0web-wgsl-encoder-ort-heads`: additionally reads back the full encoder output (`64 * 256 * 4 = 65536` bytes) and runs the tiny ORT mapped-policy/WDL heads after the WGSL encoder.
- Experimental `lc0web-wgsl-encoder-wgsl-heads`: keeps encoder+heads on GPU and reads back only final mapped policy/WDL, but still returns to CPU for legal priors and search.

## Decision on full custom inference

Do **not** promote full custom LC0 inference yet.

Next gates before an end-to-end custom runtime:

1. Add ORT comparisons for the remaining full attention block if practical.
2. Repeat and broaden alternating browser runs against ORT WebGPU after validating the full encoder stack.
3. Remove the remaining correctness-first hybrid bottlenecks before considering promotion: the scalar WGSL smolgen matmuls, final encoder-stack readback into ORT heads, and the ORT head session itself.
4. Broaden `lc0:browser-hybrid-drift` beyond the current smoke limit and repeat Chromium/WebGPU runs before comparing promotion candidates.
5. Continue replacing remaining correctness-first kernels (for example attention score/value and any profitable fusion points) with tiled/fused variants without loosening the parity gate.
5. Preserve f32 ONNX/native parity as the correctness ladder while using f16/WebGPU as deployment target.
