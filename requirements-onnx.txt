# Primary PyTorch training + ONNX export/runtime tooling for nano-LC0 residual artifacts.
# Install into the project training venv, e.g.:
#   python3 -m venv .venv-onnx
#   .venv-onnx/bin/pip install -r requirements-onnx.txt
# CPU is enough for smoke tests; use CUDA when available for real training.
torch>=2.3
onnx>=1.16
onnxruntime>=1.18
onnxscript>=0.1
onnxsim>=0.6.3
